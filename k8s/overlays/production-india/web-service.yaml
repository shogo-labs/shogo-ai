apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: studio
  namespace: shogo-production-system
  labels:
    app.kubernetes.io/part-of: shogo
    environment: production
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/min-scale: "1"
        autoscaling.knative.dev/max-scale: "10"
    spec:
      containers:
        - name: web
          image: ap-mumbai-1.ocir.io/idin4oltblww/shogo/shogo-web:production-latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          env:
            - name: API_UPSTREAM
              value: "http://api.shogo-production-system.svc.cluster.local"
            - name: API_HOST
              value: "api.shogo-production-system.svc.cluster.local"
            - name: DNS_RESOLVER
              value: "kube-dns.kube-system.svc.cluster.local"
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "200m"
