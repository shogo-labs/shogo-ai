-- Migration: add_region_to_analytics_digest
-- Generated: 2026-05-21T19:22:23.342Z by scripts/db-migrate-desktop.ts
-- Source:    prisma/schema.local.prisma
--
-- This file was generated by `bun run db:migrate:desktop --name add_region_to_analytics_digest`,
-- then TRIMMED by hand to only contain the analytics_digests changes.
--
-- The raw `prisma migrate diff --from-migrations apps/desktop/prisma/migrations
-- --to-schema prisma/schema.local.prisma --script` output also included
-- RedefineTables for ~9 tables (`agent_configs`, `agent_eval_sets`,
-- `budget_alerts`, `eval_runs`, `model_experiments`, `projects`,
-- `signup_attributions`, `subagent_model_overrides`, `usage_wallets`,
-- `workspace_grants`) which are pre-existing ACCEPTED_DRIFT entries
-- in scripts/check-desktop-schema-drift.ts. Those tables are
-- intentionally not corrected here — shipping their full Prisma-emitted
-- recreate-and-copy would be a much larger desktop change than this PR
-- is doing, and the existing ACCEPTED_DRIFT allow-list documents why
-- they're being left alone for now. Each carries a separate
-- "Detailed audit pending" follow-up.
--
-- The only schema change in THIS migration is the analytics_digests
-- column + index swap. The cloud track's matching migration is
-- prisma/migrations/20260521000000_add_region_to_analytics_digest/.

-- DropIndex
DROP INDEX "analytics_digests_date_period_key";

-- AlterTable
ALTER TABLE "analytics_digests" ADD COLUMN "region" TEXT;

-- CreateIndex
CREATE UNIQUE INDEX "analytics_digests_date_period_region_key" ON "analytics_digests"("date", "period", "region");
