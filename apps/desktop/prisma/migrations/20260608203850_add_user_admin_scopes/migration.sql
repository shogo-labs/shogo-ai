-- Migration: add_user_admin_scopes
-- Generated: 2026-06-08T20:38:51.456Z by scripts/db-migrate-desktop.ts
-- Source:    prisma/schema.local.prisma
--
-- This file was generated by `bun run db:migrate:desktop --name add_user_admin_scopes`.
-- The generator re-emitted RedefineTable churn for 8 tables (agent_configs,
-- agent_eval_sets, budget_alerts, eval_runs, model_experiments,
-- subagent_model_overrides, usage_wallets, workspace_grants) plus a
-- signup_attributions index, all of which are PRE-EXISTING drift already on
-- the ACCEPTED_DRIFT allow-list in scripts/check-desktop-schema-drift.ts.
-- Including that unrelated churn here would rewrite those tables on every
-- desktop upgrade for no reason, so it is intentionally omitted (matching the
-- 20260605010501_add_custom_domains convention).
--
-- The only intended change is users.adminScopes (granular admin permission
-- scopes, stored as a JSON-encoded TEXT list — see prisma/schema.local.prisma).
-- SQLite supports a plain ADD COLUMN here, so we use that instead of the
-- generator's destructive table rebuild.

-- AlterTable
ALTER TABLE "users" ADD COLUMN "adminScopes" TEXT NOT NULL DEFAULT '[]';
