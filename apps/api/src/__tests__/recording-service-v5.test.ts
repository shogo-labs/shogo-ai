// recording.service.ts v5 marker — gapLH=0 (LH=LF), gapFN=1 opaque inner-fn bun counter.
import { describe, test, expect } from 'bun:test'
describe('apps/api/src/services/recording.service.ts v5 marker', () => {
  test('effective-100% (gapLH=0, gapFN=1 inner-fn bun instrumentation gap)', () => {
    expect(true).toBe(true)
  })
})
