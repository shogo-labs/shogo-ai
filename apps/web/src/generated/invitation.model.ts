/**
 * Auto-generated Invitation MST Model
 *
 * Generated by @shogo/sdk - DO NOT EDIT DIRECTLY
 */

import { types, Instance, SnapshotIn, SnapshotOut } from "mobx-state-tree"

// Referenced models (use types.late() to avoid circular imports)
import { WorkspaceModel } from "./workspace.model"

// ============================================================================
// Invitation Model
// ============================================================================

export const InvitationModel = types
  .model("Invitation", {
    id: types.identifier,
    email: types.string,
    role: types.enumeration("MemberRole", ["owner", "admin", "member", "viewer"]),
    workspaceId: types.optional(types.string, ""),
    projectId: types.optional(types.string, ""),
    status: types.enumeration("InvitationStatus", ["pending", "accepted", "declined", "expired", "cancelled"]),
    emailStatus: types.enumeration("EmailStatus", ["not_sent", "sent", "failed"]),
    emailSentAt: types.optional(types.number, 0),
    emailError: types.optional(types.string, ""),
    invitedBy: types.optional(types.string, ""),
    expiresAt: types.number,
    createdAt: types.optional(types.number, 0),
    updatedAt: types.number,
    workspace: types.safeReference(types.late(() => WorkspaceModel)),
  })
  .views(self => ({
    /** Check if this is a new/unsaved entity */
    get isNew(): boolean {
      return self.id.startsWith("temp-")
    },
  }))
  .actions(self => ({
    /** Update entity fields */
    update(changes: Partial<SnapshotIn<typeof self>>) {
      Object.assign(self, changes)
    },
  }))

// Type exports
export interface IInvitation extends Instance<typeof InvitationModel> {}
export interface IInvitationSnapshotIn extends SnapshotIn<typeof InvitationModel> {}
export interface IInvitationSnapshotOut extends SnapshotOut<typeof InvitationModel> {}
