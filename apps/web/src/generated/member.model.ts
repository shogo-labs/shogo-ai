/**
 * Auto-generated Member MST Model
 *
 * Generated by @shogo-ai/sdk - DO NOT EDIT DIRECTLY
 */

import { types, Instance, SnapshotIn, SnapshotOut, IAnyModelType } from "mobx-state-tree"

// Referenced models (use types.late() to avoid circular imports)
import { UserModel } from "./user.model"
import { WorkspaceModel } from "./workspace.model"
import { ProjectModel } from "./project.model"

// ============================================================================
// Member Model
// ============================================================================

export const MemberModel = types
  .model("Member", {
    id: types.identifier,
    userId: types.string,
    role: types.enumeration("MemberRole", ["owner", "admin", "member", "viewer"]),
    workspaceId: types.optional(types.string, ""),
    projectId: types.optional(types.string, ""),
    isBillingAdmin: types.boolean,
    createdAt: types.optional(types.number, 0),
    updatedAt: types.number,
    user: types.safeReference(types.late((): IAnyModelType => UserModel)),
    workspace: types.safeReference(types.late((): IAnyModelType => WorkspaceModel)),
    project: types.safeReference(types.late((): IAnyModelType => ProjectModel)),
  })
  .views((self: any) => ({
    /** Check if this is a new/unsaved entity */
    get isNew(): boolean {
      return self.id.startsWith("temp-")
    },
  }))
  .actions((self: any) => ({
    /** Update entity fields */
    update(changes: Record<string, any>): void {
      Object.assign(self, changes)
    },
  }))

// Type exports
export interface IMember extends Instance<typeof MemberModel> {}
export interface IMemberSnapshotIn extends SnapshotIn<typeof MemberModel> {}
export interface IMemberSnapshotOut extends SnapshotOut<typeof MemberModel> {}
