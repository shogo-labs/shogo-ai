/**
 * Auto-generated Folder MST Model
 *
 * Generated by @shogo-ai/sdk - DO NOT EDIT DIRECTLY
 */

import { types, Instance, SnapshotIn, SnapshotOut, IAnyModelType } from "mobx-state-tree"

// Referenced models (use types.late() to avoid circular imports)
import { WorkspaceModel } from "./workspace.model"
import { ProjectModel } from "./project.model"

// ============================================================================
// Folder Model
// ============================================================================

export const FolderModel = types
  .model("Folder", {
    id: types.identifier,
    name: types.string,
    workspaceId: types.string,
    parentId: types.optional(types.string, ""),
    createdBy: types.optional(types.string, ""),
    createdAt: types.optional(types.number, 0),
    updatedAt: types.number,
    workspace: types.safeReference(types.late((): IAnyModelType => WorkspaceModel)),
    projects: types.optional(types.array(types.safeReference(types.late((): IAnyModelType => ProjectModel))), []),
  })
  .views((self: any) => ({
    /** Check if this is a new/unsaved entity */
    get isNew(): boolean {
      return self.id.startsWith("temp-")
    },
  }))
  .actions((self: any) => ({
    /** Update entity fields */
    update(changes: Record<string, any>): void {
      Object.assign(self, changes)
    },
  }))

// Type exports
export interface IFolder extends Instance<typeof FolderModel> {}
export interface IFolderSnapshotIn extends SnapshotIn<typeof FolderModel> {}
export interface IFolderSnapshotOut extends SnapshotOut<typeof FolderModel> {}
