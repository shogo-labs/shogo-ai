/**
 * Auto-generated Project Store
 *
 * Generated by @shogo/sdk - DO NOT EDIT DIRECTLY
 */

import { OptimisticStore } from "@shogo/sdk"
import type { HttpClient } from "@shogo/sdk"
import type { ProjectType } from "./project.types"

// Store instance (singleton)
let _store: OptimisticStore<ProjectType> | null = null

/**
 * Get or create the Project store
 *
 * @param http - HttpClient instance (required on first call)
 */
export function getProjectStore(http?: HttpClient): OptimisticStore<ProjectType> {
  if (!_store) {
    if (!http) {
      throw new Error("HttpClient required to initialize ProjectStore")
    }
    _store = new OptimisticStore<ProjectType>({
      http,
      endpoint: "/api/projects",
    })
  }
  return _store
}

/**
 * Reset the Project store (for testing)
 */
export function resetProjectStore(): void {
  _store = null
}

// Convenience export for direct import
export type { ProjectType }
