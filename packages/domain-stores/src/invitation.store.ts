/**
 * Auto-generated Invitation Store
 *
 * Generated by @shogo-ai/sdk - DO NOT EDIT DIRECTLY
 */

import { OptimisticStore } from "@shogo-ai/sdk"
import type { HttpClient } from "@shogo-ai/sdk"
import type { InvitationType } from "./invitation.types"

// Store instance (singleton)
let _store: OptimisticStore<InvitationType> | null = null

/**
 * Get or create the Invitation store
 *
 * @param http - HttpClient instance (required on first call)
 */
export function getInvitationStore(http?: HttpClient): OptimisticStore<InvitationType> {
  if (!_store) {
    if (!http) {
      throw new Error("HttpClient required to initialize InvitationStore")
    }
    _store = new OptimisticStore<InvitationType>({
      http,
      endpoint: "/api/invitations",
    })
  }
  return _store
}

/**
 * Reset the Invitation store (for testing)
 */
export function resetInvitationStore(): void {
  _store = null
}

// Convenience export for direct import
export type { InvitationType }
