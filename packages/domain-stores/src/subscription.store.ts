/**
 * Auto-generated Subscription Store
 *
 * Generated by @shogo-ai/sdk - DO NOT EDIT DIRECTLY
 */

import { OptimisticStore } from "@shogo-ai/sdk"
import type { HttpClient } from "@shogo-ai/sdk"
import type { SubscriptionType } from "./subscription.types"

// Store instance (singleton)
let _store: OptimisticStore<SubscriptionType> | null = null

/**
 * Get or create the Subscription store
 *
 * @param http - HttpClient instance (required on first call)
 */
export function getSubscriptionStore(http?: HttpClient): OptimisticStore<SubscriptionType> {
  if (!_store) {
    if (!http) {
      throw new Error("HttpClient required to initialize SubscriptionStore")
    }
    _store = new OptimisticStore<SubscriptionType>({
      http,
      endpoint: "/api/subscriptions",
    })
  }
  return _store
}

/**
 * Reset the Subscription store (for testing)
 */
export function resetSubscriptionStore(): void {
  _store = null
}

// Convenience export for direct import
export type { SubscriptionType }
