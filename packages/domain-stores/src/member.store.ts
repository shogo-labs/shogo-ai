/**
 * Auto-generated Member Store
 *
 * Generated by @shogo-ai/sdk - DO NOT EDIT DIRECTLY
 */

import { OptimisticStore } from "@shogo-ai/sdk"
import type { HttpClient } from "@shogo-ai/sdk"
import type { MemberType } from "./member.types"

// Store instance (singleton)
let _store: OptimisticStore<MemberType> | null = null

/**
 * Get or create the Member store
 *
 * @param http - HttpClient instance (required on first call)
 */
export function getMemberStore(http?: HttpClient): OptimisticStore<MemberType> {
  if (!_store) {
    if (!http) {
      throw new Error("HttpClient required to initialize MemberStore")
    }
    _store = new OptimisticStore<MemberType>({
      http,
      endpoint: "/api/members",
    })
  }
  return _store
}

/**
 * Reset the Member store (for testing)
 */
export function resetMemberStore(): void {
  _store = null
}

// Convenience export for direct import
export type { MemberType }
