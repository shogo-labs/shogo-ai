// SPDX-License-Identifier: AGPL-3.0-or-later
// Copyright (C) 2026 Shogo Technologies, Inc.
/**
 * Auto-generated ChatSession MST Model
 *
 * Generated by @shogo-ai/sdk - DO NOT EDIT DIRECTLY
 */

import { types, Instance, SnapshotIn, SnapshotOut } from "mobx-state-tree"

// Referenced models (use types.late() to avoid circular imports)
import { ProjectModel } from "./project.model"
import { ChatMessageModel } from "./chat-message.model"
import { ToolCallLogModel } from "./tool-call-log.model"

// ============================================================================
// ChatSession Model
// ============================================================================

export const ChatSessionModel = types
  .model("ChatSession", {
    id: types.identifier,
    name: types.optional(types.string, ""),
    inferredName: types.string,
    contextType: types.enumeration("ContextType", ["feature", "project", "general"]),
    contextId: types.optional(types.string, ""),
    phase: types.optional(types.string, ""),
    claudeCodeSessionId: types.optional(types.string, ""),
    cachedMessageCount: types.optional(types.number, 0),
    contextUsageTokens: types.optional(types.number, 0),
    contextWindowTokens: types.optional(types.number, 0),
    createdAt: types.optional(types.number, 0),
    updatedAt: types.optional(types.number, 0),
    lastActiveAt: types.optional(types.number, 0),
    project: types.safeReference(types.late(() => ProjectModel)),
    messages: types.optional(types.array(types.safeReference(types.late(() => ChatMessageModel))), []),
    toolCallLogs: types.optional(types.array(types.safeReference(types.late(() => ToolCallLogModel))), []),
  })
  .views(self => ({
    /** Check if this is a new/unsaved entity */
    get isNew(): boolean {
      return self.id.startsWith("temp-")
    },
  }))
  .actions(self => ({
    /** Update entity fields */
    update(changes: Partial<SnapshotIn<typeof self>>) {
      Object.assign(self, changes)
    },
  }))

// Type exports
export interface IChatSession extends Instance<typeof ChatSessionModel> {}
export interface IChatSessionSnapshotIn extends SnapshotIn<typeof ChatSessionModel> {}
export interface IChatSessionSnapshotOut extends SnapshotOut<typeof ChatSessionModel> {}
