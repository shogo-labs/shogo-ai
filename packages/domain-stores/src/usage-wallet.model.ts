// SPDX-License-Identifier: AGPL-3.0-or-later
// Copyright (C) 2026 Shogo Technologies, Inc.
/**
 * Auto-generated UsageWallet MST Model
 *
 * Generated by @shogo-ai/sdk - DO NOT EDIT DIRECTLY
 */

import { types, Instance, SnapshotIn, SnapshotOut } from "mobx-state-tree"

// Referenced models (use types.late() to avoid circular imports)
import { WorkspaceModel } from "./workspace.model"

// ============================================================================
// UsageWallet Model
// ============================================================================

export const UsageWalletModel = types
  .model("UsageWallet", {
    id: types.identifier,
    workspaceId: types.string,
    monthlyIncludedUsd: types.optional(types.number, 0),
    dailyIncludedUsd: types.optional(types.number, 0),
    monthlyIncludedAllocationUsd: types.optional(types.number, 0),
    dailyUsedThisMonthUsd: types.optional(types.number, 0),
    overageEnabled: types.optional(types.boolean, false),
    overageHardLimitUsd: types.optional(types.number, 0),
    overageAccumulatedUsd: types.optional(types.number, 0),
    stripeMeteredItemId: types.optional(types.string, ""),
    anniversaryDay: types.number,
    lastDailyReset: types.number,
    lastMonthlyReset: types.number,
    createdAt: types.optional(types.number, 0),
    updatedAt: types.number,
    workspace: types.safeReference(types.late(() => WorkspaceModel)),
  })
  .views(self => ({
    /** Check if this is a new/unsaved entity */
    get isNew(): boolean {
      return self.id.startsWith("temp-")
    },
  }))
  .actions(self => ({
    /** Update entity fields */
    update(changes: Partial<SnapshotIn<typeof self>>) {
      Object.assign(self, changes)
    },
  }))

// Type exports
export interface IUsageWallet extends Instance<typeof UsageWalletModel> {}
export interface IUsageWalletSnapshotIn extends SnapshotIn<typeof UsageWalletModel> {}
export interface IUsageWalletSnapshotOut extends SnapshotOut<typeof UsageWalletModel> {}
