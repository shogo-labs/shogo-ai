// SPDX-License-Identifier: AGPL-3.0-or-later
// Copyright (C) 2026 Shogo Technologies, Inc.
// @ts-nocheck
/**
 * Auto-generated UsageEvent MST Model
 *
 * Generated by @shogo-ai/sdk - DO NOT EDIT DIRECTLY
 */

import { types, Instance, SnapshotIn, SnapshotOut } from "mobx-state-tree"

// Referenced models (use types.late() to avoid circular imports)
import { WorkspaceModel } from "./workspace.model"
import { ProjectModel } from "./project.model"

// ============================================================================
// UsageEvent Model
// ============================================================================

export const UsageEventModel = types
  .model("UsageEvent", {
    id: types.identifier,
    workspaceId: types.string,
    projectId: types.optional(types.string, ""),
    memberId: types.string,
    actionType: types.string,
    actionMetadata: types.optional(types.frozen(), {}),
    creditCost: types.number,
    creditSource: types.enumeration("CreditSource", ["daily", "monthly"]),
    balanceBefore: types.number,
    balanceAfter: types.number,
    createdAt: types.optional(types.number, 0),
    workspace: types.safeReference(types.late(() => WorkspaceModel)),
    project: types.safeReference(types.late(() => ProjectModel)),
  })
  .views(self => ({
    /** Check if this is a new/unsaved entity */
    get isNew(): boolean {
      return self.id.startsWith("temp-")
    },
  }))
  .actions(self => ({
    /** Update entity fields */
    update(changes: Partial<SnapshotIn<typeof self>>) {
      Object.assign(self, changes)
    },
  }))

// Type exports
export interface IUsageEvent extends Instance<typeof UsageEventModel> {}
export interface IUsageEventSnapshotIn extends SnapshotIn<typeof UsageEventModel> {}
export interface IUsageEventSnapshotOut extends SnapshotOut<typeof UsageEventModel> {}
