// @ts-nocheck
/**
 * Auto-generated Notification MST Model
 *
 * Generated by @shogo-ai/sdk - DO NOT EDIT DIRECTLY
 */

import { types, Instance, SnapshotIn, SnapshotOut } from "mobx-state-tree"

// Referenced models (use types.late() to avoid circular imports)
import { UserModel } from "./user.model"

// ============================================================================
// Notification Model
// ============================================================================

export const NotificationModel = types
  .model("Notification", {
    id: types.identifier,
    userId: types.string,
    type: types.enumeration("NotificationType", ["invitation_pending", "invitation_accepted", "member_joined", "member_left", "workspace_updated"]),
    title: types.string,
    message: types.string,
    metadata: types.optional(types.frozen(), {}),
    actionUrl: types.optional(types.string, ""),
    readAt: types.optional(types.number, 0),
    createdAt: types.optional(types.number, 0),
    user: types.safeReference(types.late(() => UserModel)),
  })
  .views(self => ({
    /** Check if this is a new/unsaved entity */
    get isNew(): boolean {
      return self.id.startsWith("temp-")
    },
  }))
  .actions(self => ({
    /** Update entity fields */
    update(changes: Partial<SnapshotIn<typeof self>>) {
      Object.assign(self, changes)
    },
  }))

// Type exports
export interface INotification extends Instance<typeof NotificationModel> {}
export interface INotificationSnapshotIn extends SnapshotIn<typeof NotificationModel> {}
export interface INotificationSnapshotOut extends SnapshotOut<typeof NotificationModel> {}
