// SPDX-License-Identifier: MIT
// Copyright (C) 2026 Shogo Technologies, Inc.
/**
 * Auto-generated ProjectFolder MST Model
 *
 * Generated by @shogo-ai/sdk - DO NOT EDIT DIRECTLY
 */

import { types, Instance, SnapshotIn, SnapshotOut } from "mobx-state-tree"

// Referenced models (use types.late() to avoid circular imports)
import { ProjectModel } from "./project.model"

// ============================================================================
// ProjectFolder Model
// ============================================================================

export const ProjectFolderModel = types
  .model("ProjectFolder", {
    id: types.identifier,
    projectId: types.string,
    path: types.string,
    isPrimary: types.optional(types.boolean, false),
    addedAt: types.optional(types.number, 0),
    lastOpenedAt: types.optional(types.number, 0),
    project: types.safeReference(types.late(() => ProjectModel)),
  })
  .views(self => ({
    /** Check if this is a new/unsaved entity */
    get isNew(): boolean {
      return self.id.startsWith("temp-")
    },
  }))
  .actions(self => ({
    /** Update entity fields */
    update(changes: Partial<SnapshotIn<typeof self>>) {
      Object.assign(self, changes)
    },
  }))

// Type exports
export interface IProjectFolder extends Instance<typeof ProjectFolderModel> {}
export interface IProjectFolderSnapshotIn extends SnapshotIn<typeof ProjectFolderModel> {}
export interface IProjectFolderSnapshotOut extends SnapshotOut<typeof ProjectFolderModel> {}
