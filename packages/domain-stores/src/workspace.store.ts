/**
 * Auto-generated Workspace Store
 *
 * Generated by @shogo-ai/sdk - DO NOT EDIT DIRECTLY
 */

import { OptimisticStore } from "@shogo-ai/sdk"
import type { HttpClient } from "@shogo-ai/sdk"
import type { WorkspaceType } from "./workspace.types"

// Store instance (singleton)
let _store: OptimisticStore<WorkspaceType> | null = null

/**
 * Get or create the Workspace store
 *
 * @param http - HttpClient instance (required on first call)
 */
export function getWorkspaceStore(http?: HttpClient): OptimisticStore<WorkspaceType> {
  if (!_store) {
    if (!http) {
      throw new Error("HttpClient required to initialize WorkspaceStore")
    }
    _store = new OptimisticStore<WorkspaceType>({
      http,
      endpoint: "/api/workspaces",
    })
  }
  return _store
}

/**
 * Reset the Workspace store (for testing)
 */
export function resetWorkspaceStore(): void {
  _store = null
}

// Convenience export for direct import
export type { WorkspaceType }
