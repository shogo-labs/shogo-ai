/**
 * Auto-generated CreditLedger Store
 *
 * Generated by @shogo-ai/sdk - DO NOT EDIT DIRECTLY
 */

import { OptimisticStore } from "@shogo-ai/sdk"
import type { HttpClient } from "@shogo-ai/sdk"
import type { CreditLedgerType } from "./credit-ledger.types"

// Store instance (singleton)
let _store: OptimisticStore<CreditLedgerType> | null = null

/**
 * Get or create the CreditLedger store
 *
 * @param http - HttpClient instance (required on first call)
 */
export function getCreditLedgerStore(http?: HttpClient): OptimisticStore<CreditLedgerType> {
  if (!_store) {
    if (!http) {
      throw new Error("HttpClient required to initialize CreditLedgerStore")
    }
    _store = new OptimisticStore<CreditLedgerType>({
      http,
      endpoint: "/api/credit-ledgers",
    })
  }
  return _store
}

/**
 * Reset the CreditLedger store (for testing)
 */
export function resetCreditLedgerStore(): void {
  _store = null
}

// Convenience export for direct import
export type { CreditLedgerType }
