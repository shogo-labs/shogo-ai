{
  "name": "@shogo/pty-core",
  "version": "0.0.0",
  "private": true,
  "description": "Shared PTY primitives (scrollback ring, OSC decoder, protocol types). Consumed by apps/desktop pty-host AND packages/agent-runtime. Pure TS, no runtime deps.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  }
}
