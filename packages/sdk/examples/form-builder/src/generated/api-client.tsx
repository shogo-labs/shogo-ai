/**
 * Auto-generated API Client
 *
 * Typed HTTP client for CRUD operations.
 * Generated by `shogo generate` - DO NOT EDIT
 */

import type {
  UserType,
  UserCreateInput,
  UserUpdateInput,
  FormType,
  FormCreateInput,
  FormUpdateInput,
  FieldType,
  FieldCreateInput,
  FieldUpdateInput,
  SubmissionType,
  SubmissionCreateInput,
  SubmissionUpdateInput,
  ResponseType,
  ResponseCreateInput,
  ResponseUpdateInput,
} from './types'

// ============================================================================
// Response Types
// ============================================================================

export interface ApiResponse<T> {
  ok: boolean
  data?: T
  items?: T[]
  error?: { code: string; message: string }
}

// ============================================================================
// Client Configuration
// ============================================================================

export interface ApiClientConfig {
  /** Base URL for API requests (e.g., "http://localhost:3000/api") */
  baseUrl: string
  /** Optional auth token to include in requests */
  token?: string
  /** Optional user ID to include in requests */
  userId?: string
}

let config: ApiClientConfig = { baseUrl: '/api' }

/**
 * Configure the API client
 */
export function configureApiClient(newConfig: Partial<ApiClientConfig>) {
  config = { ...config, ...newConfig }
}

/**
 * Get current API client configuration
 */
export function getApiConfig(): ApiClientConfig {
  return { ...config }
}

// ============================================================================
// HTTP Helpers
// ============================================================================

async function request<T>(
  method: string,
  path: string,
  body?: unknown
): Promise<ApiResponse<T>> {
  const headers: Record<string, string> = {
    'Content-Type': 'application/json',
  }

  if (config.token) {
    headers['Authorization'] = `Bearer ${config.token}`
  }

  const url = `${config.baseUrl}${path}`

  try {
    const response = await fetch(url, {
      method,
      headers,
      body: body ? JSON.stringify(body) : undefined,
    })

    const data = await response.json()

    if (!response.ok) {
      return {
        ok: false,
        error: data.error || { code: 'request_failed', message: `HTTP ${response.status}` },
      }
    }

    return data
  } catch (error) {
    return {
      ok: false,
      error: {
        code: 'network_error',
        message: error instanceof Error ? error.message : 'Network error',
      },
    }
  }
}

// ============================================================================
// User API Client
// ============================================================================

export const userApi = {
  /**
   * List all User records
   * @param options - Query options including where filters, pagination, and custom query params
   * @param options.where - Filter conditions (passed as query params to the API)
   * @param options.limit - Maximum number of records to return
   * @param options.offset - Number of records to skip
   * @param options.params - Additional query parameters to append to the request
   */
  async list(options?: { where?: Record<string, unknown>; limit?: number; offset?: number; params?: Record<string, string | number | boolean> }): Promise<ApiResponse<UserType[]>> {
    const params = new URLSearchParams()
    
    // Add where filters as query params
    if (options?.where) {
      for (const [key, value] of Object.entries(options.where)) {
        if (value !== undefined && value !== null) {
          params.set(key, String(value))
        }
      }
    }
    
    // Add additional params
    if (options?.params) {
      for (const [key, value] of Object.entries(options.params)) {
        if (value !== undefined && value !== null) {
          params.set(key, String(value))
        }
      }
    }
    
    // Add pagination
    if (options?.limit) params.set('limit', String(options.limit))
    if (options?.offset) params.set('offset', String(options.offset))
    if (config.userId) params.set('userId', config.userId)
    
    const query = params.toString() ? `?${params.toString()}` : ''
    const result = await request<UserType>('GET', `/users${query}`)
    return { ok: result.ok, items: result.items, error: result.error }
  },

  /**
   * Get a single User by ID
   */
  async get(id: string): Promise<ApiResponse<UserType>> {
    return request<UserType>('GET', `/users/${id}`)
  },

  /**
   * Create a new User
   */
  async create(input: UserCreateInput): Promise<ApiResponse<UserType>> {
    const body = config.userId ? { ...input, userId: config.userId } : input
    return request<UserType>('POST', `/users`, body)
  },

  /**
   * Update an existing User
   */
  async update(id: string, input: UserUpdateInput): Promise<ApiResponse<UserType>> {
    return request<UserType>('PATCH', `/users/${id}`, input)
  },

  /**
   * Delete a User
   */
  async delete(id: string): Promise<ApiResponse<void>> {
    return request<void>('DELETE', `/users/${id}`)
  },
}

// ============================================================================
// Form API Client
// ============================================================================

export const formApi = {
  /**
   * List all Form records
   * @param options - Query options including where filters, pagination, and custom query params
   * @param options.where - Filter conditions (passed as query params to the API)
   * @param options.limit - Maximum number of records to return
   * @param options.offset - Number of records to skip
   * @param options.params - Additional query parameters to append to the request
   */
  async list(options?: { where?: Record<string, unknown>; limit?: number; offset?: number; params?: Record<string, string | number | boolean> }): Promise<ApiResponse<FormType[]>> {
    const params = new URLSearchParams()
    
    // Add where filters as query params
    if (options?.where) {
      for (const [key, value] of Object.entries(options.where)) {
        if (value !== undefined && value !== null) {
          params.set(key, String(value))
        }
      }
    }
    
    // Add additional params
    if (options?.params) {
      for (const [key, value] of Object.entries(options.params)) {
        if (value !== undefined && value !== null) {
          params.set(key, String(value))
        }
      }
    }
    
    // Add pagination
    if (options?.limit) params.set('limit', String(options.limit))
    if (options?.offset) params.set('offset', String(options.offset))
    if (config.userId) params.set('userId', config.userId)
    
    const query = params.toString() ? `?${params.toString()}` : ''
    const result = await request<FormType>('GET', `/forms${query}`)
    return { ok: result.ok, items: result.items, error: result.error }
  },

  /**
   * Get a single Form by ID
   */
  async get(id: string): Promise<ApiResponse<FormType>> {
    return request<FormType>('GET', `/forms/${id}`)
  },

  /**
   * Create a new Form
   */
  async create(input: FormCreateInput): Promise<ApiResponse<FormType>> {
    const body = config.userId ? { ...input, userId: config.userId } : input
    return request<FormType>('POST', `/forms`, body)
  },

  /**
   * Update an existing Form
   */
  async update(id: string, input: FormUpdateInput): Promise<ApiResponse<FormType>> {
    return request<FormType>('PATCH', `/forms/${id}`, input)
  },

  /**
   * Delete a Form
   */
  async delete(id: string): Promise<ApiResponse<void>> {
    return request<void>('DELETE', `/forms/${id}`)
  },
}

// ============================================================================
// Field API Client
// ============================================================================

export const fieldApi = {
  /**
   * List all Field records
   * @param options - Query options including where filters, pagination, and custom query params
   * @param options.where - Filter conditions (passed as query params to the API)
   * @param options.limit - Maximum number of records to return
   * @param options.offset - Number of records to skip
   * @param options.params - Additional query parameters to append to the request
   */
  async list(options?: { where?: Record<string, unknown>; limit?: number; offset?: number; params?: Record<string, string | number | boolean> }): Promise<ApiResponse<FieldType[]>> {
    const params = new URLSearchParams()
    
    // Add where filters as query params
    if (options?.where) {
      for (const [key, value] of Object.entries(options.where)) {
        if (value !== undefined && value !== null) {
          params.set(key, String(value))
        }
      }
    }
    
    // Add additional params
    if (options?.params) {
      for (const [key, value] of Object.entries(options.params)) {
        if (value !== undefined && value !== null) {
          params.set(key, String(value))
        }
      }
    }
    
    // Add pagination
    if (options?.limit) params.set('limit', String(options.limit))
    if (options?.offset) params.set('offset', String(options.offset))
    if (config.userId) params.set('userId', config.userId)
    
    const query = params.toString() ? `?${params.toString()}` : ''
    const result = await request<FieldType>('GET', `/fields${query}`)
    return { ok: result.ok, items: result.items, error: result.error }
  },

  /**
   * Get a single Field by ID
   */
  async get(id: string): Promise<ApiResponse<FieldType>> {
    return request<FieldType>('GET', `/fields/${id}`)
  },

  /**
   * Create a new Field
   */
  async create(input: FieldCreateInput): Promise<ApiResponse<FieldType>> {
    const body = config.userId ? { ...input, userId: config.userId } : input
    return request<FieldType>('POST', `/fields`, body)
  },

  /**
   * Update an existing Field
   */
  async update(id: string, input: FieldUpdateInput): Promise<ApiResponse<FieldType>> {
    return request<FieldType>('PATCH', `/fields/${id}`, input)
  },

  /**
   * Delete a Field
   */
  async delete(id: string): Promise<ApiResponse<void>> {
    return request<void>('DELETE', `/fields/${id}`)
  },
}

// ============================================================================
// Submission API Client
// ============================================================================

export const submissionApi = {
  /**
   * List all Submission records
   * @param options - Query options including where filters, pagination, and custom query params
   * @param options.where - Filter conditions (passed as query params to the API)
   * @param options.limit - Maximum number of records to return
   * @param options.offset - Number of records to skip
   * @param options.params - Additional query parameters to append to the request
   */
  async list(options?: { where?: Record<string, unknown>; limit?: number; offset?: number; params?: Record<string, string | number | boolean> }): Promise<ApiResponse<SubmissionType[]>> {
    const params = new URLSearchParams()
    
    // Add where filters as query params
    if (options?.where) {
      for (const [key, value] of Object.entries(options.where)) {
        if (value !== undefined && value !== null) {
          params.set(key, String(value))
        }
      }
    }
    
    // Add additional params
    if (options?.params) {
      for (const [key, value] of Object.entries(options.params)) {
        if (value !== undefined && value !== null) {
          params.set(key, String(value))
        }
      }
    }
    
    // Add pagination
    if (options?.limit) params.set('limit', String(options.limit))
    if (options?.offset) params.set('offset', String(options.offset))
    if (config.userId) params.set('userId', config.userId)
    
    const query = params.toString() ? `?${params.toString()}` : ''
    const result = await request<SubmissionType>('GET', `/submissions${query}`)
    return { ok: result.ok, items: result.items, error: result.error }
  },

  /**
   * Get a single Submission by ID
   */
  async get(id: string): Promise<ApiResponse<SubmissionType>> {
    return request<SubmissionType>('GET', `/submissions/${id}`)
  },

  /**
   * Create a new Submission
   */
  async create(input: SubmissionCreateInput): Promise<ApiResponse<SubmissionType>> {
    const body = config.userId ? { ...input, userId: config.userId } : input
    return request<SubmissionType>('POST', `/submissions`, body)
  },

  /**
   * Update an existing Submission
   */
  async update(id: string, input: SubmissionUpdateInput): Promise<ApiResponse<SubmissionType>> {
    return request<SubmissionType>('PATCH', `/submissions/${id}`, input)
  },

  /**
   * Delete a Submission
   */
  async delete(id: string): Promise<ApiResponse<void>> {
    return request<void>('DELETE', `/submissions/${id}`)
  },
}

// ============================================================================
// Response API Client
// ============================================================================

export const responseApi = {
  /**
   * List all Response records
   * @param options - Query options including where filters, pagination, and custom query params
   * @param options.where - Filter conditions (passed as query params to the API)
   * @param options.limit - Maximum number of records to return
   * @param options.offset - Number of records to skip
   * @param options.params - Additional query parameters to append to the request
   */
  async list(options?: { where?: Record<string, unknown>; limit?: number; offset?: number; params?: Record<string, string | number | boolean> }): Promise<ApiResponse<ResponseType[]>> {
    const params = new URLSearchParams()
    
    // Add where filters as query params
    if (options?.where) {
      for (const [key, value] of Object.entries(options.where)) {
        if (value !== undefined && value !== null) {
          params.set(key, String(value))
        }
      }
    }
    
    // Add additional params
    if (options?.params) {
      for (const [key, value] of Object.entries(options.params)) {
        if (value !== undefined && value !== null) {
          params.set(key, String(value))
        }
      }
    }
    
    // Add pagination
    if (options?.limit) params.set('limit', String(options.limit))
    if (options?.offset) params.set('offset', String(options.offset))
    if (config.userId) params.set('userId', config.userId)
    
    const query = params.toString() ? `?${params.toString()}` : ''
    const result = await request<ResponseType>('GET', `/responses${query}`)
    return { ok: result.ok, items: result.items, error: result.error }
  },

  /**
   * Get a single Response by ID
   */
  async get(id: string): Promise<ApiResponse<ResponseType>> {
    return request<ResponseType>('GET', `/responses/${id}`)
  },

  /**
   * Create a new Response
   */
  async create(input: ResponseCreateInput): Promise<ApiResponse<ResponseType>> {
    const body = config.userId ? { ...input, userId: config.userId } : input
    return request<ResponseType>('POST', `/responses`, body)
  },

  /**
   * Update an existing Response
   */
  async update(id: string, input: ResponseUpdateInput): Promise<ApiResponse<ResponseType>> {
    return request<ResponseType>('PATCH', `/responses/${id}`, input)
  },

  /**
   * Delete a Response
   */
  async delete(id: string): Promise<ApiResponse<void>> {
    return request<void>('DELETE', `/responses/${id}`)
  },
}

// ============================================================================
// Combined API Client
// ============================================================================

/**
 * Combined API client with all model endpoints
 */
export const api = {
  user: userApi,
  form: formApi,
  field: fieldApi,
  submission: submissionApi,
  response: responseApi,
}

export default api
