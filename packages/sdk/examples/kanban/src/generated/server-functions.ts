/**
 * Auto-generated Server Functions
 *
 * CRUD operations for TanStack Start.
 * Generated by `shogo generate` - DO NOT EDIT
 */

import { createServerFn } from '@tanstack/react-start'
import { shogo } from '../lib/shogo'
import { hooks } from './hooks'
import type {
  UserType,
  UserCreateInput,
  UserUpdateInput,
  BoardType,
  BoardCreateInput,
  BoardUpdateInput,
  ColumnType,
  ColumnCreateInput,
  ColumnUpdateInput,
  CardType,
  CardCreateInput,
  CardUpdateInput,
  LabelType,
  LabelCreateInput,
  LabelUpdateInput,
  CardLabelType,
  CardLabelCreateInput,
  CardLabelUpdateInput,
} from './types'

// ============================================================================
// User Server Functions
// ============================================================================

/**
 * List all User records
 */
export const getUserList = createServerFn({ method: 'POST' })
  .inputValidator((data: { userId?: string; where?: Record<string, unknown> }) => data)
  .handler(async ({ data }) => {
    let where: Record<string, unknown> = data.where || {}

    // Apply beforeList hook if defined
    const hook = hooks.User?.beforeList
    if (hook) {
      const hookResult = await hook({ userId: data.userId })
      if (hookResult?.where) {
        where = { ...where, ...hookResult.where }
      }
    }

    const items = await shogo.db.user.findMany({
      where,
      orderBy: { createdAt: 'desc' },
    })

    return items as UserType[]
  })

/**
 * Get a single User by ID
 */
export const getUserById = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeGet hook if defined
    const hook = hooks.User?.beforeGet
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    const item = await shogo.db.user.findUnique({
      where: { id: data.id },
    })

    if (!item) {
      throw new Error('User not found')
    }

    return item as UserType
  })

/**
 * Create a new User
 */
export const createUser = createServerFn({ method: 'POST' })
  .inputValidator((data: { input: UserCreateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeCreate hook if defined
    const hook = hooks.User?.beforeCreate
    if (hook) {
      const hookResult = await hook(input, { userId: data.userId })
      if (hookResult) {
        input = hookResult
      }
    }

    const item = await shogo.db.user.create({
      data: input,
    })

    // Apply afterCreate hook if defined
    const afterHook = hooks.User?.afterCreate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as UserType
  })

/**
 * Update an existing User
 */
export const updateUser = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; input: UserUpdateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeUpdate hook if defined
    const hook = hooks.User?.beforeUpdate
    if (hook) {
      const hookResult = await hook(data.id, input, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
      if (hookResult?.input) {
        input = hookResult.input
      }
    }

    const item = await shogo.db.user.update({
      where: { id: data.id },
      data: input,
    })

    // Apply afterUpdate hook if defined
    const afterHook = hooks.User?.afterUpdate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as UserType
  })

/**
 * Delete a User
 */
export const deleteUser = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeDelete hook if defined
    const hook = hooks.User?.beforeDelete
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    await shogo.db.user.delete({
      where: { id: data.id },
    })

    // Apply afterDelete hook if defined
    const afterHook = hooks.User?.afterDelete
    if (afterHook) {
      await afterHook(data.id, { userId: data.userId })
    }

    return { success: true }
  })

// ============================================================================
// Board Server Functions
// ============================================================================

/**
 * List all Board records
 */
export const getBoardList = createServerFn({ method: 'POST' })
  .inputValidator((data: { userId?: string; where?: Record<string, unknown> }) => data)
  .handler(async ({ data }) => {
    let where: Record<string, unknown> = data.where || {}

    // Apply beforeList hook if defined
    const hook = hooks.Board?.beforeList
    if (hook) {
      const hookResult = await hook({ userId: data.userId })
      if (hookResult?.where) {
        where = { ...where, ...hookResult.where }
      }
    }

    const items = await shogo.db.board.findMany({
      where,
      orderBy: { createdAt: 'desc' },
    })

    return items as BoardType[]
  })

/**
 * Get a single Board by ID
 */
export const getBoardById = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeGet hook if defined
    const hook = hooks.Board?.beforeGet
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    const item = await shogo.db.board.findUnique({
      where: { id: data.id },
    })

    if (!item) {
      throw new Error('Board not found')
    }

    return item as BoardType
  })

/**
 * Create a new Board
 */
export const createBoard = createServerFn({ method: 'POST' })
  .inputValidator((data: { input: BoardCreateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeCreate hook if defined
    const hook = hooks.Board?.beforeCreate
    if (hook) {
      const hookResult = await hook(input, { userId: data.userId })
      if (hookResult) {
        input = hookResult
      }
    }

    const item = await shogo.db.board.create({
      data: input,
    })

    // Apply afterCreate hook if defined
    const afterHook = hooks.Board?.afterCreate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as BoardType
  })

/**
 * Update an existing Board
 */
export const updateBoard = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; input: BoardUpdateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeUpdate hook if defined
    const hook = hooks.Board?.beforeUpdate
    if (hook) {
      const hookResult = await hook(data.id, input, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
      if (hookResult?.input) {
        input = hookResult.input
      }
    }

    const item = await shogo.db.board.update({
      where: { id: data.id },
      data: input,
    })

    // Apply afterUpdate hook if defined
    const afterHook = hooks.Board?.afterUpdate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as BoardType
  })

/**
 * Delete a Board
 */
export const deleteBoard = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeDelete hook if defined
    const hook = hooks.Board?.beforeDelete
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    await shogo.db.board.delete({
      where: { id: data.id },
    })

    // Apply afterDelete hook if defined
    const afterHook = hooks.Board?.afterDelete
    if (afterHook) {
      await afterHook(data.id, { userId: data.userId })
    }

    return { success: true }
  })

// ============================================================================
// Column Server Functions
// ============================================================================

/**
 * List all Column records
 */
export const getColumnList = createServerFn({ method: 'POST' })
  .inputValidator((data: { userId?: string; where?: Record<string, unknown> }) => data)
  .handler(async ({ data }) => {
    let where: Record<string, unknown> = data.where || {}

    // Apply beforeList hook if defined
    const hook = hooks.Column?.beforeList
    if (hook) {
      const hookResult = await hook({ userId: data.userId })
      if (hookResult?.where) {
        where = { ...where, ...hookResult.where }
      }
    }

    const items = await shogo.db.column.findMany({
      where,
      orderBy: { createdAt: 'desc' },
    })

    return items as ColumnType[]
  })

/**
 * Get a single Column by ID
 */
export const getColumnById = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeGet hook if defined
    const hook = hooks.Column?.beforeGet
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    const item = await shogo.db.column.findUnique({
      where: { id: data.id },
    })

    if (!item) {
      throw new Error('Column not found')
    }

    return item as ColumnType
  })

/**
 * Create a new Column
 */
export const createColumn = createServerFn({ method: 'POST' })
  .inputValidator((data: { input: ColumnCreateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeCreate hook if defined
    const hook = hooks.Column?.beforeCreate
    if (hook) {
      const hookResult = await hook(input, { userId: data.userId })
      if (hookResult) {
        input = hookResult
      }
    }

    const item = await shogo.db.column.create({
      data: input,
    })

    // Apply afterCreate hook if defined
    const afterHook = hooks.Column?.afterCreate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as ColumnType
  })

/**
 * Update an existing Column
 */
export const updateColumn = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; input: ColumnUpdateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeUpdate hook if defined
    const hook = hooks.Column?.beforeUpdate
    if (hook) {
      const hookResult = await hook(data.id, input, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
      if (hookResult?.input) {
        input = hookResult.input
      }
    }

    const item = await shogo.db.column.update({
      where: { id: data.id },
      data: input,
    })

    // Apply afterUpdate hook if defined
    const afterHook = hooks.Column?.afterUpdate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as ColumnType
  })

/**
 * Delete a Column
 */
export const deleteColumn = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeDelete hook if defined
    const hook = hooks.Column?.beforeDelete
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    await shogo.db.column.delete({
      where: { id: data.id },
    })

    // Apply afterDelete hook if defined
    const afterHook = hooks.Column?.afterDelete
    if (afterHook) {
      await afterHook(data.id, { userId: data.userId })
    }

    return { success: true }
  })

// ============================================================================
// Card Server Functions
// ============================================================================

/**
 * List all Card records
 */
export const getCardList = createServerFn({ method: 'POST' })
  .inputValidator((data: { userId?: string; where?: Record<string, unknown> }) => data)
  .handler(async ({ data }) => {
    let where: Record<string, unknown> = data.where || {}

    // Apply beforeList hook if defined
    const hook = hooks.Card?.beforeList
    if (hook) {
      const hookResult = await hook({ userId: data.userId })
      if (hookResult?.where) {
        where = { ...where, ...hookResult.where }
      }
    }

    const items = await shogo.db.card.findMany({
      where,
      orderBy: { createdAt: 'desc' },
    })

    return items as CardType[]
  })

/**
 * Get a single Card by ID
 */
export const getCardById = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeGet hook if defined
    const hook = hooks.Card?.beforeGet
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    const item = await shogo.db.card.findUnique({
      where: { id: data.id },
    })

    if (!item) {
      throw new Error('Card not found')
    }

    return item as CardType
  })

/**
 * Create a new Card
 */
export const createCard = createServerFn({ method: 'POST' })
  .inputValidator((data: { input: CardCreateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeCreate hook if defined
    const hook = hooks.Card?.beforeCreate
    if (hook) {
      const hookResult = await hook(input, { userId: data.userId })
      if (hookResult) {
        input = hookResult
      }
    }

    const item = await shogo.db.card.create({
      data: input,
    })

    // Apply afterCreate hook if defined
    const afterHook = hooks.Card?.afterCreate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as CardType
  })

/**
 * Update an existing Card
 */
export const updateCard = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; input: CardUpdateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeUpdate hook if defined
    const hook = hooks.Card?.beforeUpdate
    if (hook) {
      const hookResult = await hook(data.id, input, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
      if (hookResult?.input) {
        input = hookResult.input
      }
    }

    const item = await shogo.db.card.update({
      where: { id: data.id },
      data: input,
    })

    // Apply afterUpdate hook if defined
    const afterHook = hooks.Card?.afterUpdate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as CardType
  })

/**
 * Delete a Card
 */
export const deleteCard = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeDelete hook if defined
    const hook = hooks.Card?.beforeDelete
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    await shogo.db.card.delete({
      where: { id: data.id },
    })

    // Apply afterDelete hook if defined
    const afterHook = hooks.Card?.afterDelete
    if (afterHook) {
      await afterHook(data.id, { userId: data.userId })
    }

    return { success: true }
  })

// ============================================================================
// Label Server Functions
// ============================================================================

/**
 * List all Label records
 */
export const getLabelList = createServerFn({ method: 'POST' })
  .inputValidator((data: { userId?: string; where?: Record<string, unknown> }) => data)
  .handler(async ({ data }) => {
    let where: Record<string, unknown> = data.where || {}

    // Apply beforeList hook if defined
    const hook = hooks.Label?.beforeList
    if (hook) {
      const hookResult = await hook({ userId: data.userId })
      if (hookResult?.where) {
        where = { ...where, ...hookResult.where }
      }
    }

    const items = await shogo.db.label.findMany({
      where,
      orderBy: { createdAt: 'desc' },
    })

    return items as LabelType[]
  })

/**
 * Get a single Label by ID
 */
export const getLabelById = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeGet hook if defined
    const hook = hooks.Label?.beforeGet
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    const item = await shogo.db.label.findUnique({
      where: { id: data.id },
    })

    if (!item) {
      throw new Error('Label not found')
    }

    return item as LabelType
  })

/**
 * Create a new Label
 */
export const createLabel = createServerFn({ method: 'POST' })
  .inputValidator((data: { input: LabelCreateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeCreate hook if defined
    const hook = hooks.Label?.beforeCreate
    if (hook) {
      const hookResult = await hook(input, { userId: data.userId })
      if (hookResult) {
        input = hookResult
      }
    }

    const item = await shogo.db.label.create({
      data: input,
    })

    // Apply afterCreate hook if defined
    const afterHook = hooks.Label?.afterCreate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as LabelType
  })

/**
 * Update an existing Label
 */
export const updateLabel = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; input: LabelUpdateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeUpdate hook if defined
    const hook = hooks.Label?.beforeUpdate
    if (hook) {
      const hookResult = await hook(data.id, input, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
      if (hookResult?.input) {
        input = hookResult.input
      }
    }

    const item = await shogo.db.label.update({
      where: { id: data.id },
      data: input,
    })

    // Apply afterUpdate hook if defined
    const afterHook = hooks.Label?.afterUpdate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as LabelType
  })

/**
 * Delete a Label
 */
export const deleteLabel = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeDelete hook if defined
    const hook = hooks.Label?.beforeDelete
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    await shogo.db.label.delete({
      where: { id: data.id },
    })

    // Apply afterDelete hook if defined
    const afterHook = hooks.Label?.afterDelete
    if (afterHook) {
      await afterHook(data.id, { userId: data.userId })
    }

    return { success: true }
  })

// Skipped CardLabel - no @id field found
