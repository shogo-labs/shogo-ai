/**
 * Auto-generated Server Functions
 *
 * CRUD operations for TanStack Start.
 * Generated by `shogo generate` - DO NOT EDIT
 */

import { createServerFn } from '@tanstack/react-start'
import { shogo } from '../lib/shogo'
import { hooks } from './hooks'
import type {
  UserType,
  UserCreateInput,
  UserUpdateInput,
  ServiceType,
  ServiceCreateInput,
  ServiceUpdateInput,
  TimeSlotType,
  TimeSlotCreateInput,
  TimeSlotUpdateInput,
  BookingType,
  BookingCreateInput,
  BookingUpdateInput,
} from './types'

// ============================================================================
// User Server Functions
// ============================================================================

/**
 * List all User records
 */
export const getUserList = createServerFn({ method: 'POST' })
  .inputValidator((data: { userId?: string; where?: Record<string, unknown> }) => data)
  .handler(async ({ data }) => {
    let where: Record<string, unknown> = data.where || {}

    // Apply beforeList hook if defined
    const hook = hooks.User?.beforeList
    if (hook) {
      const hookResult = await hook({ userId: data.userId })
      if (hookResult?.where) {
        where = { ...where, ...hookResult.where }
      }
    }

    const items = await shogo.db.user.findMany({
      where,
      orderBy: { createdAt: 'desc' },
    })

    return items as UserType[]
  })

/**
 * Get a single User by ID
 */
export const getUserById = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeGet hook if defined
    const hook = hooks.User?.beforeGet
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    const item = await shogo.db.user.findUnique({
      where: { id: data.id },
    })

    if (!item) {
      throw new Error('User not found')
    }

    return item as UserType
  })

/**
 * Create a new User
 */
export const createUser = createServerFn({ method: 'POST' })
  .inputValidator((data: { input: UserCreateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeCreate hook if defined
    const hook = hooks.User?.beforeCreate
    if (hook) {
      const hookResult = await hook(input, { userId: data.userId })
      if (hookResult) {
        input = hookResult
      }
    }

    const item = await shogo.db.user.create({
      data: input,
    })

    // Apply afterCreate hook if defined
    const afterHook = hooks.User?.afterCreate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as UserType
  })

/**
 * Update an existing User
 */
export const updateUser = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; input: UserUpdateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeUpdate hook if defined
    const hook = hooks.User?.beforeUpdate
    if (hook) {
      const hookResult = await hook(data.id, input, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
      if (hookResult?.input) {
        input = hookResult.input
      }
    }

    const item = await shogo.db.user.update({
      where: { id: data.id },
      data: input,
    })

    // Apply afterUpdate hook if defined
    const afterHook = hooks.User?.afterUpdate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as UserType
  })

/**
 * Delete a User
 */
export const deleteUser = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeDelete hook if defined
    const hook = hooks.User?.beforeDelete
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    await shogo.db.user.delete({
      where: { id: data.id },
    })

    // Apply afterDelete hook if defined
    const afterHook = hooks.User?.afterDelete
    if (afterHook) {
      await afterHook(data.id, { userId: data.userId })
    }

    return { success: true }
  })

// ============================================================================
// Service Server Functions
// ============================================================================

/**
 * List all Service records
 */
export const getServiceList = createServerFn({ method: 'POST' })
  .inputValidator((data: { userId?: string; where?: Record<string, unknown> }) => data)
  .handler(async ({ data }) => {
    let where: Record<string, unknown> = data.where || {}

    // Apply beforeList hook if defined
    const hook = hooks.Service?.beforeList
    if (hook) {
      const hookResult = await hook({ userId: data.userId })
      if (hookResult?.where) {
        where = { ...where, ...hookResult.where }
      }
    }

    const items = await shogo.db.service.findMany({
      where,
      orderBy: { createdAt: 'desc' },
    })

    return items as ServiceType[]
  })

/**
 * Get a single Service by ID
 */
export const getServiceById = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeGet hook if defined
    const hook = hooks.Service?.beforeGet
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    const item = await shogo.db.service.findUnique({
      where: { id: data.id },
    })

    if (!item) {
      throw new Error('Service not found')
    }

    return item as ServiceType
  })

/**
 * Create a new Service
 */
export const createService = createServerFn({ method: 'POST' })
  .inputValidator((data: { input: ServiceCreateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeCreate hook if defined
    const hook = hooks.Service?.beforeCreate
    if (hook) {
      const hookResult = await hook(input, { userId: data.userId })
      if (hookResult) {
        input = hookResult
      }
    }

    const item = await shogo.db.service.create({
      data: input,
    })

    // Apply afterCreate hook if defined
    const afterHook = hooks.Service?.afterCreate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as ServiceType
  })

/**
 * Update an existing Service
 */
export const updateService = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; input: ServiceUpdateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeUpdate hook if defined
    const hook = hooks.Service?.beforeUpdate
    if (hook) {
      const hookResult = await hook(data.id, input, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
      if (hookResult?.input) {
        input = hookResult.input
      }
    }

    const item = await shogo.db.service.update({
      where: { id: data.id },
      data: input,
    })

    // Apply afterUpdate hook if defined
    const afterHook = hooks.Service?.afterUpdate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as ServiceType
  })

/**
 * Delete a Service
 */
export const deleteService = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeDelete hook if defined
    const hook = hooks.Service?.beforeDelete
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    await shogo.db.service.delete({
      where: { id: data.id },
    })

    // Apply afterDelete hook if defined
    const afterHook = hooks.Service?.afterDelete
    if (afterHook) {
      await afterHook(data.id, { userId: data.userId })
    }

    return { success: true }
  })

// ============================================================================
// TimeSlot Server Functions
// ============================================================================

/**
 * List all TimeSlot records
 */
export const getTimeSlotList = createServerFn({ method: 'POST' })
  .inputValidator((data: { userId?: string; where?: Record<string, unknown> }) => data)
  .handler(async ({ data }) => {
    let where: Record<string, unknown> = data.where || {}

    // Apply beforeList hook if defined
    const hook = hooks.TimeSlot?.beforeList
    if (hook) {
      const hookResult = await hook({ userId: data.userId })
      if (hookResult?.where) {
        where = { ...where, ...hookResult.where }
      }
    }

    const items = await shogo.db.timeSlot.findMany({
      where,
      orderBy: { createdAt: 'desc' },
    })

    return items as TimeSlotType[]
  })

/**
 * Get a single TimeSlot by ID
 */
export const getTimeSlotById = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeGet hook if defined
    const hook = hooks.TimeSlot?.beforeGet
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    const item = await shogo.db.timeSlot.findUnique({
      where: { id: data.id },
    })

    if (!item) {
      throw new Error('TimeSlot not found')
    }

    return item as TimeSlotType
  })

/**
 * Create a new TimeSlot
 */
export const createTimeSlot = createServerFn({ method: 'POST' })
  .inputValidator((data: { input: TimeSlotCreateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeCreate hook if defined
    const hook = hooks.TimeSlot?.beforeCreate
    if (hook) {
      const hookResult = await hook(input, { userId: data.userId })
      if (hookResult) {
        input = hookResult
      }
    }

    const item = await shogo.db.timeSlot.create({
      data: input,
    })

    // Apply afterCreate hook if defined
    const afterHook = hooks.TimeSlot?.afterCreate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as TimeSlotType
  })

/**
 * Update an existing TimeSlot
 */
export const updateTimeSlot = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; input: TimeSlotUpdateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeUpdate hook if defined
    const hook = hooks.TimeSlot?.beforeUpdate
    if (hook) {
      const hookResult = await hook(data.id, input, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
      if (hookResult?.input) {
        input = hookResult.input
      }
    }

    const item = await shogo.db.timeSlot.update({
      where: { id: data.id },
      data: input,
    })

    // Apply afterUpdate hook if defined
    const afterHook = hooks.TimeSlot?.afterUpdate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as TimeSlotType
  })

/**
 * Delete a TimeSlot
 */
export const deleteTimeSlot = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeDelete hook if defined
    const hook = hooks.TimeSlot?.beforeDelete
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    await shogo.db.timeSlot.delete({
      where: { id: data.id },
    })

    // Apply afterDelete hook if defined
    const afterHook = hooks.TimeSlot?.afterDelete
    if (afterHook) {
      await afterHook(data.id, { userId: data.userId })
    }

    return { success: true }
  })

// ============================================================================
// Booking Server Functions
// ============================================================================

/**
 * List all Booking records
 */
export const getBookingList = createServerFn({ method: 'POST' })
  .inputValidator((data: { userId?: string; where?: Record<string, unknown> }) => data)
  .handler(async ({ data }) => {
    let where: Record<string, unknown> = data.where || {}

    // Apply beforeList hook if defined
    const hook = hooks.Booking?.beforeList
    if (hook) {
      const hookResult = await hook({ userId: data.userId })
      if (hookResult?.where) {
        where = { ...where, ...hookResult.where }
      }
    }

    const items = await shogo.db.booking.findMany({
      where,
      orderBy: { createdAt: 'desc' },
    })

    return items as BookingType[]
  })

/**
 * Get a single Booking by ID
 */
export const getBookingById = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeGet hook if defined
    const hook = hooks.Booking?.beforeGet
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    const item = await shogo.db.booking.findUnique({
      where: { id: data.id },
    })

    if (!item) {
      throw new Error('Booking not found')
    }

    return item as BookingType
  })

/**
 * Create a new Booking
 */
export const createBooking = createServerFn({ method: 'POST' })
  .inputValidator((data: { input: BookingCreateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeCreate hook if defined
    const hook = hooks.Booking?.beforeCreate
    if (hook) {
      const hookResult = await hook(input, { userId: data.userId })
      if (hookResult) {
        input = hookResult
      }
    }

    const item = await shogo.db.booking.create({
      data: input,
    })

    // Apply afterCreate hook if defined
    const afterHook = hooks.Booking?.afterCreate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as BookingType
  })

/**
 * Update an existing Booking
 */
export const updateBooking = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; input: BookingUpdateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeUpdate hook if defined
    const hook = hooks.Booking?.beforeUpdate
    if (hook) {
      const hookResult = await hook(data.id, input, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
      if (hookResult?.input) {
        input = hookResult.input
      }
    }

    const item = await shogo.db.booking.update({
      where: { id: data.id },
      data: input,
    })

    // Apply afterUpdate hook if defined
    const afterHook = hooks.Booking?.afterUpdate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as BookingType
  })

/**
 * Delete a Booking
 */
export const deleteBooking = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeDelete hook if defined
    const hook = hooks.Booking?.beforeDelete
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    await shogo.db.booking.delete({
      where: { id: data.id },
    })

    // Apply afterDelete hook if defined
    const afterHook = hooks.Booking?.afterDelete
    if (afterHook) {
      await afterHook(data.id, { userId: data.userId })
    }

    return { success: true }
  })
