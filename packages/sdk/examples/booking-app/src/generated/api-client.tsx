/**
 * Auto-generated API Client
 *
 * Typed HTTP client for CRUD operations.
 * Generated by `shogo generate` - DO NOT EDIT
 */

import type {
  UserType,
  UserCreateInput,
  UserUpdateInput,
  ServiceType,
  ServiceCreateInput,
  ServiceUpdateInput,
  TimeSlotType,
  TimeSlotCreateInput,
  TimeSlotUpdateInput,
  BookingType,
  BookingCreateInput,
  BookingUpdateInput,
} from './types'

// ============================================================================
// Response Types
// ============================================================================

export interface ApiResponse<T> {
  ok: boolean
  data?: T
  items?: T[]
  error?: { code: string; message: string }
}

// ============================================================================
// Client Configuration
// ============================================================================

export interface ApiClientConfig {
  /** Base URL for API requests (e.g., "http://localhost:3000/api") */
  baseUrl: string
  /** Optional auth token to include in requests */
  token?: string
  /** Optional user ID to include in requests */
  userId?: string
}

let config: ApiClientConfig = { baseUrl: '/api' }

/**
 * Configure the API client
 */
export function configureApiClient(newConfig: Partial<ApiClientConfig>) {
  config = { ...config, ...newConfig }
}

/**
 * Get current API client configuration
 */
export function getApiConfig(): ApiClientConfig {
  return { ...config }
}

// ============================================================================
// HTTP Helpers
// ============================================================================

async function request<T>(
  method: string,
  path: string,
  body?: unknown
): Promise<ApiResponse<T>> {
  const headers: Record<string, string> = {
    'Content-Type': 'application/json',
  }

  if (config.token) {
    headers['Authorization'] = `Bearer ${config.token}`
  }

  const url = `${config.baseUrl}${path}`

  try {
    const response = await fetch(url, {
      method,
      headers,
      body: body ? JSON.stringify(body) : undefined,
    })

    const data = await response.json()

    if (!response.ok) {
      return {
        ok: false,
        error: data.error || { code: 'request_failed', message: `HTTP ${response.status}` },
      }
    }

    return data
  } catch (error) {
    return {
      ok: false,
      error: {
        code: 'network_error',
        message: error instanceof Error ? error.message : 'Network error',
      },
    }
  }
}

// ============================================================================
// User API Client
// ============================================================================

export const userApi = {
  /**
   * List all User records
   * @param options - Query options including where filters, pagination, and custom query params
   * @param options.where - Filter conditions (passed as query params to the API)
   * @param options.limit - Maximum number of records to return
   * @param options.offset - Number of records to skip
   * @param options.params - Additional query parameters to append to the request
   */
  async list(options?: { where?: Record<string, unknown>; limit?: number; offset?: number; params?: Record<string, string | number | boolean> }): Promise<ApiResponse<UserType[]>> {
    const params = new URLSearchParams()
    
    // Add where filters as query params
    if (options?.where) {
      for (const [key, value] of Object.entries(options.where)) {
        if (value !== undefined && value !== null) {
          params.set(key, String(value))
        }
      }
    }
    
    // Add additional params
    if (options?.params) {
      for (const [key, value] of Object.entries(options.params)) {
        if (value !== undefined && value !== null) {
          params.set(key, String(value))
        }
      }
    }
    
    // Add pagination
    if (options?.limit) params.set('limit', String(options.limit))
    if (options?.offset) params.set('offset', String(options.offset))
    if (config.userId) params.set('userId', config.userId)
    
    const query = params.toString() ? `?${params.toString()}` : ''
    const result = await request<UserType>('GET', `/users${query}`)
    return { ok: result.ok, items: result.items, error: result.error }
  },

  /**
   * Get a single User by ID
   */
  async get(id: string): Promise<ApiResponse<UserType>> {
    return request<UserType>('GET', `/users/${id}`)
  },

  /**
   * Create a new User
   */
  async create(input: UserCreateInput): Promise<ApiResponse<UserType>> {
    const body = config.userId ? { ...input, userId: config.userId } : input
    return request<UserType>('POST', `/users`, body)
  },

  /**
   * Update an existing User
   */
  async update(id: string, input: UserUpdateInput): Promise<ApiResponse<UserType>> {
    return request<UserType>('PATCH', `/users/${id}`, input)
  },

  /**
   * Delete a User
   */
  async delete(id: string): Promise<ApiResponse<void>> {
    return request<void>('DELETE', `/users/${id}`)
  },
}

// ============================================================================
// Service API Client
// ============================================================================

export const serviceApi = {
  /**
   * List all Service records
   * @param options - Query options including where filters, pagination, and custom query params
   * @param options.where - Filter conditions (passed as query params to the API)
   * @param options.limit - Maximum number of records to return
   * @param options.offset - Number of records to skip
   * @param options.params - Additional query parameters to append to the request
   */
  async list(options?: { where?: Record<string, unknown>; limit?: number; offset?: number; params?: Record<string, string | number | boolean> }): Promise<ApiResponse<ServiceType[]>> {
    const params = new URLSearchParams()
    
    // Add where filters as query params
    if (options?.where) {
      for (const [key, value] of Object.entries(options.where)) {
        if (value !== undefined && value !== null) {
          params.set(key, String(value))
        }
      }
    }
    
    // Add additional params
    if (options?.params) {
      for (const [key, value] of Object.entries(options.params)) {
        if (value !== undefined && value !== null) {
          params.set(key, String(value))
        }
      }
    }
    
    // Add pagination
    if (options?.limit) params.set('limit', String(options.limit))
    if (options?.offset) params.set('offset', String(options.offset))
    if (config.userId) params.set('userId', config.userId)
    
    const query = params.toString() ? `?${params.toString()}` : ''
    const result = await request<ServiceType>('GET', `/services${query}`)
    return { ok: result.ok, items: result.items, error: result.error }
  },

  /**
   * Get a single Service by ID
   */
  async get(id: string): Promise<ApiResponse<ServiceType>> {
    return request<ServiceType>('GET', `/services/${id}`)
  },

  /**
   * Create a new Service
   */
  async create(input: ServiceCreateInput): Promise<ApiResponse<ServiceType>> {
    const body = config.userId ? { ...input, userId: config.userId } : input
    return request<ServiceType>('POST', `/services`, body)
  },

  /**
   * Update an existing Service
   */
  async update(id: string, input: ServiceUpdateInput): Promise<ApiResponse<ServiceType>> {
    return request<ServiceType>('PATCH', `/services/${id}`, input)
  },

  /**
   * Delete a Service
   */
  async delete(id: string): Promise<ApiResponse<void>> {
    return request<void>('DELETE', `/services/${id}`)
  },
}

// ============================================================================
// TimeSlot API Client
// ============================================================================

export const timeSlotApi = {
  /**
   * List all TimeSlot records
   * @param options - Query options including where filters, pagination, and custom query params
   * @param options.where - Filter conditions (passed as query params to the API)
   * @param options.limit - Maximum number of records to return
   * @param options.offset - Number of records to skip
   * @param options.params - Additional query parameters to append to the request
   */
  async list(options?: { where?: Record<string, unknown>; limit?: number; offset?: number; params?: Record<string, string | number | boolean> }): Promise<ApiResponse<TimeSlotType[]>> {
    const params = new URLSearchParams()
    
    // Add where filters as query params
    if (options?.where) {
      for (const [key, value] of Object.entries(options.where)) {
        if (value !== undefined && value !== null) {
          params.set(key, String(value))
        }
      }
    }
    
    // Add additional params
    if (options?.params) {
      for (const [key, value] of Object.entries(options.params)) {
        if (value !== undefined && value !== null) {
          params.set(key, String(value))
        }
      }
    }
    
    // Add pagination
    if (options?.limit) params.set('limit', String(options.limit))
    if (options?.offset) params.set('offset', String(options.offset))
    if (config.userId) params.set('userId', config.userId)
    
    const query = params.toString() ? `?${params.toString()}` : ''
    const result = await request<TimeSlotType>('GET', `/time-slots${query}`)
    return { ok: result.ok, items: result.items, error: result.error }
  },

  /**
   * Get a single TimeSlot by ID
   */
  async get(id: string): Promise<ApiResponse<TimeSlotType>> {
    return request<TimeSlotType>('GET', `/time-slots/${id}`)
  },

  /**
   * Create a new TimeSlot
   */
  async create(input: TimeSlotCreateInput): Promise<ApiResponse<TimeSlotType>> {
    const body = config.userId ? { ...input, userId: config.userId } : input
    return request<TimeSlotType>('POST', `/time-slots`, body)
  },

  /**
   * Update an existing TimeSlot
   */
  async update(id: string, input: TimeSlotUpdateInput): Promise<ApiResponse<TimeSlotType>> {
    return request<TimeSlotType>('PATCH', `/time-slots/${id}`, input)
  },

  /**
   * Delete a TimeSlot
   */
  async delete(id: string): Promise<ApiResponse<void>> {
    return request<void>('DELETE', `/time-slots/${id}`)
  },
}

// ============================================================================
// Booking API Client
// ============================================================================

export const bookingApi = {
  /**
   * List all Booking records
   * @param options - Query options including where filters, pagination, and custom query params
   * @param options.where - Filter conditions (passed as query params to the API)
   * @param options.limit - Maximum number of records to return
   * @param options.offset - Number of records to skip
   * @param options.params - Additional query parameters to append to the request
   */
  async list(options?: { where?: Record<string, unknown>; limit?: number; offset?: number; params?: Record<string, string | number | boolean> }): Promise<ApiResponse<BookingType[]>> {
    const params = new URLSearchParams()
    
    // Add where filters as query params
    if (options?.where) {
      for (const [key, value] of Object.entries(options.where)) {
        if (value !== undefined && value !== null) {
          params.set(key, String(value))
        }
      }
    }
    
    // Add additional params
    if (options?.params) {
      for (const [key, value] of Object.entries(options.params)) {
        if (value !== undefined && value !== null) {
          params.set(key, String(value))
        }
      }
    }
    
    // Add pagination
    if (options?.limit) params.set('limit', String(options.limit))
    if (options?.offset) params.set('offset', String(options.offset))
    if (config.userId) params.set('userId', config.userId)
    
    const query = params.toString() ? `?${params.toString()}` : ''
    const result = await request<BookingType>('GET', `/bookings${query}`)
    return { ok: result.ok, items: result.items, error: result.error }
  },

  /**
   * Get a single Booking by ID
   */
  async get(id: string): Promise<ApiResponse<BookingType>> {
    return request<BookingType>('GET', `/bookings/${id}`)
  },

  /**
   * Create a new Booking
   */
  async create(input: BookingCreateInput): Promise<ApiResponse<BookingType>> {
    const body = config.userId ? { ...input, userId: config.userId } : input
    return request<BookingType>('POST', `/bookings`, body)
  },

  /**
   * Update an existing Booking
   */
  async update(id: string, input: BookingUpdateInput): Promise<ApiResponse<BookingType>> {
    return request<BookingType>('PATCH', `/bookings/${id}`, input)
  },

  /**
   * Delete a Booking
   */
  async delete(id: string): Promise<ApiResponse<void>> {
    return request<void>('DELETE', `/bookings/${id}`)
  },
}

// ============================================================================
// Combined API Client
// ============================================================================

/**
 * Combined API client with all model endpoints
 */
export const api = {
  user: userApi,
  service: serviceApi,
  timeSlot: timeSlotApi,
  booking: bookingApi,
}

export default api
