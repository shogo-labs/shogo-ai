# Database connection
DATABASE_URL="postgresql://project:project_dev@localhost:5433/ai_chat"

# Shogo AI Proxy (preferred - no raw API keys needed)
AI_PROXY_URL=http://localhost:8002/api/ai/v1
AI_PROXY_TOKEN=<generate-via-api-server>

# Direct Anthropic API key (fallback for local dev without proxy)
# ANTHROPIC_API_KEY=sk-ant-...
