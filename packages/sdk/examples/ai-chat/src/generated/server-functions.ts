/**
 * Auto-generated Server Functions
 *
 * CRUD operations for TanStack Start.
 * Generated by `shogo generate` - DO NOT EDIT
 */

import { createServerFn } from '@tanstack/react-start'
import { shogo } from '../lib/shogo'
import { hooks } from './hooks'
import type {
  UserType,
  UserCreateInput,
  UserUpdateInput,
  ChatType,
  ChatCreateInput,
  ChatUpdateInput,
  MessageType,
  MessageCreateInput,
  MessageUpdateInput,
} from './types'

// ============================================================================
// User Server Functions
// ============================================================================

/**
 * List all User records
 */
export const getUserList = createServerFn({ method: 'POST' })
  .inputValidator((data: { userId?: string; where?: Record<string, unknown> }) => data)
  .handler(async ({ data }) => {
    let where: Record<string, unknown> = data.where || {}

    // Apply beforeList hook if defined
    const hook = hooks.User?.beforeList
    if (hook) {
      const hookResult = await hook({ userId: data.userId })
      if (hookResult?.where) {
        where = { ...where, ...hookResult.where }
      }
    }

    const items = await shogo.db.user.findMany({
      where,
      orderBy: { createdAt: 'desc' },
    })

    return items as UserType[]
  })

/**
 * Get a single User by ID
 */
export const getUserById = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeGet hook if defined
    const hook = hooks.User?.beforeGet
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    const item = await shogo.db.user.findUnique({
      where: { id: data.id },
    })

    if (!item) {
      throw new Error('User not found')
    }

    return item as UserType
  })

/**
 * Create a new User
 */
export const createUser = createServerFn({ method: 'POST' })
  .inputValidator((data: { input: UserCreateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeCreate hook if defined
    const hook = hooks.User?.beforeCreate
    if (hook) {
      const hookResult = await hook(input, { userId: data.userId })
      if (hookResult) {
        input = hookResult
      }
    }

    const item = await shogo.db.user.create({
      data: input,
    })

    // Apply afterCreate hook if defined
    const afterHook = hooks.User?.afterCreate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as UserType
  })

/**
 * Update an existing User
 */
export const updateUser = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; input: UserUpdateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeUpdate hook if defined
    const hook = hooks.User?.beforeUpdate
    if (hook) {
      const hookResult = await hook(data.id, input, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
      if (hookResult?.input) {
        input = hookResult.input
      }
    }

    const item = await shogo.db.user.update({
      where: { id: data.id },
      data: input,
    })

    // Apply afterUpdate hook if defined
    const afterHook = hooks.User?.afterUpdate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as UserType
  })

/**
 * Delete a User
 */
export const deleteUser = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeDelete hook if defined
    const hook = hooks.User?.beforeDelete
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    await shogo.db.user.delete({
      where: { id: data.id },
    })

    // Apply afterDelete hook if defined
    const afterHook = hooks.User?.afterDelete
    if (afterHook) {
      await afterHook(data.id, { userId: data.userId })
    }

    return { success: true }
  })

// ============================================================================
// Chat Server Functions
// ============================================================================

/**
 * List all Chat records
 */
export const getChatList = createServerFn({ method: 'POST' })
  .inputValidator((data: { userId?: string; where?: Record<string, unknown> }) => data)
  .handler(async ({ data }) => {
    let where: Record<string, unknown> = data.where || {}

    // Apply beforeList hook if defined
    const hook = hooks.Chat?.beforeList
    if (hook) {
      const hookResult = await hook({ userId: data.userId })
      if (hookResult?.where) {
        where = { ...where, ...hookResult.where }
      }
    }

    const items = await shogo.db.chat.findMany({
      where,
      orderBy: { createdAt: 'desc' },
    })

    return items as ChatType[]
  })

/**
 * Get a single Chat by ID
 */
export const getChatById = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeGet hook if defined
    const hook = hooks.Chat?.beforeGet
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    const item = await shogo.db.chat.findUnique({
      where: { id: data.id },
    })

    if (!item) {
      throw new Error('Chat not found')
    }

    return item as ChatType
  })

/**
 * Create a new Chat
 */
export const createChat = createServerFn({ method: 'POST' })
  .inputValidator((data: { input: ChatCreateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeCreate hook if defined
    const hook = hooks.Chat?.beforeCreate
    if (hook) {
      const hookResult = await hook(input, { userId: data.userId })
      if (hookResult) {
        input = hookResult
      }
    }

    const item = await shogo.db.chat.create({
      data: input,
    })

    // Apply afterCreate hook if defined
    const afterHook = hooks.Chat?.afterCreate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as ChatType
  })

/**
 * Update an existing Chat
 */
export const updateChat = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; input: ChatUpdateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeUpdate hook if defined
    const hook = hooks.Chat?.beforeUpdate
    if (hook) {
      const hookResult = await hook(data.id, input, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
      if (hookResult?.input) {
        input = hookResult.input
      }
    }

    const item = await shogo.db.chat.update({
      where: { id: data.id },
      data: input,
    })

    // Apply afterUpdate hook if defined
    const afterHook = hooks.Chat?.afterUpdate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as ChatType
  })

/**
 * Delete a Chat
 */
export const deleteChat = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeDelete hook if defined
    const hook = hooks.Chat?.beforeDelete
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    await shogo.db.chat.delete({
      where: { id: data.id },
    })

    // Apply afterDelete hook if defined
    const afterHook = hooks.Chat?.afterDelete
    if (afterHook) {
      await afterHook(data.id, { userId: data.userId })
    }

    return { success: true }
  })

// ============================================================================
// Message Server Functions
// ============================================================================

/**
 * List all Message records
 */
export const getMessageList = createServerFn({ method: 'POST' })
  .inputValidator((data: { userId?: string; where?: Record<string, unknown> }) => data)
  .handler(async ({ data }) => {
    let where: Record<string, unknown> = data.where || {}

    // Apply beforeList hook if defined
    const hook = hooks.Message?.beforeList
    if (hook) {
      const hookResult = await hook({ userId: data.userId })
      if (hookResult?.where) {
        where = { ...where, ...hookResult.where }
      }
    }

    const items = await shogo.db.message.findMany({
      where,
      orderBy: { createdAt: 'desc' },
    })

    return items as MessageType[]
  })

/**
 * Get a single Message by ID
 */
export const getMessageById = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeGet hook if defined
    const hook = hooks.Message?.beforeGet
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    const item = await shogo.db.message.findUnique({
      where: { id: data.id },
    })

    if (!item) {
      throw new Error('Message not found')
    }

    return item as MessageType
  })

/**
 * Create a new Message
 */
export const createMessage = createServerFn({ method: 'POST' })
  .inputValidator((data: { input: MessageCreateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeCreate hook if defined
    const hook = hooks.Message?.beforeCreate
    if (hook) {
      const hookResult = await hook(input, { userId: data.userId })
      if (hookResult) {
        input = hookResult
      }
    }

    const item = await shogo.db.message.create({
      data: input,
    })

    // Apply afterCreate hook if defined
    const afterHook = hooks.Message?.afterCreate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as MessageType
  })

/**
 * Update an existing Message
 */
export const updateMessage = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; input: MessageUpdateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeUpdate hook if defined
    const hook = hooks.Message?.beforeUpdate
    if (hook) {
      const hookResult = await hook(data.id, input, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
      if (hookResult?.input) {
        input = hookResult.input
      }
    }

    const item = await shogo.db.message.update({
      where: { id: data.id },
      data: input,
    })

    // Apply afterUpdate hook if defined
    const afterHook = hooks.Message?.afterUpdate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as MessageType
  })

/**
 * Delete a Message
 */
export const deleteMessage = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeDelete hook if defined
    const hook = hooks.Message?.beforeDelete
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    await shogo.db.message.delete({
      where: { id: data.id },
    })

    // Apply afterDelete hook if defined
    const afterHook = hooks.Message?.afterDelete
    if (afterHook) {
      await afterHook(data.id, { userId: data.userId })
    }

    return { success: true }
  })
