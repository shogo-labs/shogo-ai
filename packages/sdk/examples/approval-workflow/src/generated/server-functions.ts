/**
 * Auto-generated Client Functions
 *
 * Fetch-based CRUD operations that call the Hono REST API.
 * No server-side imports — safe for browser bundles.
 * Generated by `shogo generate` - DO NOT EDIT
 */

import type {
  UserType,
  UserCreateInput,
  UserUpdateInput,
  ApprovalRequestType,
  ApprovalRequestCreateInput,
  ApprovalRequestUpdateInput,
  ApprovalStepType,
  ApprovalStepCreateInput,
  ApprovalStepUpdateInput,
  CommentType,
  CommentCreateInput,
  CommentUpdateInput,
} from './types'

/** Get the API base URL */
function getApiBase(): string {
  if (typeof window !== 'undefined') {
    return window.location.origin
  }
  return process.env.API_URL || 'http://localhost:3001'
}

// ============================================================================
// User Client Functions
// ============================================================================

/**
 * List all User records
 */
export async function getUserList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<UserType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/users${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list User')
  }
  const json = await response.json()
  return (json.items || []) as UserType[]
}

/**
 * Get a single User by ID
 */
export async function getUserById(args: { data: { id: string; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'User not found')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Create a new User
 */
export async function createUser(args: { data: { input: UserCreateInput; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create User')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Update an existing User
 */
export async function updateUser(args: { data: { id: string; input: UserUpdateInput; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update User')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Delete a User
 */
export async function deleteUser(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete User')
  }
  return { success: true }
}

// ============================================================================
// ApprovalRequest Client Functions
// ============================================================================

/**
 * List all ApprovalRequest records
 */
export async function getApprovalRequestList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<ApprovalRequestType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/approval-requests${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list ApprovalRequest')
  }
  const json = await response.json()
  return (json.items || []) as ApprovalRequestType[]
}

/**
 * Get a single ApprovalRequest by ID
 */
export async function getApprovalRequestById(args: { data: { id: string; userId?: string } }): Promise<ApprovalRequestType> {
  const url = `${getApiBase()}/api/approval-requests/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'ApprovalRequest not found')
  }
  const json = await response.json()
  return json.data as ApprovalRequestType
}

/**
 * Create a new ApprovalRequest
 */
export async function createApprovalRequest(args: { data: { input: ApprovalRequestCreateInput; userId?: string } }): Promise<ApprovalRequestType> {
  const url = `${getApiBase()}/api/approval-requests`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create ApprovalRequest')
  }
  const json = await response.json()
  return json.data as ApprovalRequestType
}

/**
 * Update an existing ApprovalRequest
 */
export async function updateApprovalRequest(args: { data: { id: string; input: ApprovalRequestUpdateInput; userId?: string } }): Promise<ApprovalRequestType> {
  const url = `${getApiBase()}/api/approval-requests/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update ApprovalRequest')
  }
  const json = await response.json()
  return json.data as ApprovalRequestType
}

/**
 * Delete a ApprovalRequest
 */
export async function deleteApprovalRequest(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/approval-requests/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete ApprovalRequest')
  }
  return { success: true }
}

// ============================================================================
// ApprovalStep Client Functions
// ============================================================================

/**
 * List all ApprovalStep records
 */
export async function getApprovalStepList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<ApprovalStepType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/approval-steps${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list ApprovalStep')
  }
  const json = await response.json()
  return (json.items || []) as ApprovalStepType[]
}

/**
 * Get a single ApprovalStep by ID
 */
export async function getApprovalStepById(args: { data: { id: string; userId?: string } }): Promise<ApprovalStepType> {
  const url = `${getApiBase()}/api/approval-steps/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'ApprovalStep not found')
  }
  const json = await response.json()
  return json.data as ApprovalStepType
}

/**
 * Create a new ApprovalStep
 */
export async function createApprovalStep(args: { data: { input: ApprovalStepCreateInput; userId?: string } }): Promise<ApprovalStepType> {
  const url = `${getApiBase()}/api/approval-steps`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create ApprovalStep')
  }
  const json = await response.json()
  return json.data as ApprovalStepType
}

/**
 * Update an existing ApprovalStep
 */
export async function updateApprovalStep(args: { data: { id: string; input: ApprovalStepUpdateInput; userId?: string } }): Promise<ApprovalStepType> {
  const url = `${getApiBase()}/api/approval-steps/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update ApprovalStep')
  }
  const json = await response.json()
  return json.data as ApprovalStepType
}

/**
 * Delete a ApprovalStep
 */
export async function deleteApprovalStep(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/approval-steps/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete ApprovalStep')
  }
  return { success: true }
}

// ============================================================================
// Comment Client Functions
// ============================================================================

/**
 * List all Comment records
 */
export async function getCommentList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<CommentType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/comments${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Comment')
  }
  const json = await response.json()
  return (json.items || []) as CommentType[]
}

/**
 * Get a single Comment by ID
 */
export async function getCommentById(args: { data: { id: string; userId?: string } }): Promise<CommentType> {
  const url = `${getApiBase()}/api/comments/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Comment not found')
  }
  const json = await response.json()
  return json.data as CommentType
}

/**
 * Create a new Comment
 */
export async function createComment(args: { data: { input: CommentCreateInput; userId?: string } }): Promise<CommentType> {
  const url = `${getApiBase()}/api/comments`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Comment')
  }
  const json = await response.json()
  return json.data as CommentType
}

/**
 * Update an existing Comment
 */
export async function updateComment(args: { data: { id: string; input: CommentUpdateInput; userId?: string } }): Promise<CommentType> {
  const url = `${getApiBase()}/api/comments/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Comment')
  }
  const json = await response.json()
  return json.data as CommentType
}

/**
 * Delete a Comment
 */
export async function deleteComment(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/comments/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Comment')
  }
  return { success: true }
}
