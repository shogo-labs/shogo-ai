/**
 * Auto-generated ApprovalRequest Routes
 *
 * Generated by @shogo-ai/sdk - DO NOT EDIT DIRECTLY
 */

import { Hono } from "hono"
import { PrismaClient } from "./prisma/client"
import type { ApprovalRequestHooks } from "./approvalrequest.hooks"

let prisma: PrismaClient | null = null
let hooks: ApprovalRequestHooks = {}

export function setPrisma(client: PrismaClient) { prisma = client }
export function setApprovalRequestHooks(h: ApprovalRequestHooks) { hooks = h }

function getPrisma(): PrismaClient {
  if (!prisma) throw new Error("Prisma client not set. Call setPrisma() first.")
  return prisma
}

function buildContext(c: any, body?: any) {
  return {
    body: body || {},
    params: c.req.param() || {},
    query: Object.fromEntries(new URL(c.req.url).searchParams),
    userId: c.get("auth")?.userId,
    prisma: getPrisma(),
  }
}

export function createApprovalRequestRoutes(): Hono {
  const router = new Hono()

  router.get("/", async (c) => {
    try {
      const ctx = buildContext(c)
      const p = getPrisma()
      const query = ctx.query
      const reservedParams = ["limit", "offset", "include", "orderBy"]
      let where: any = {}
      for (const [key, value] of Object.entries(query)) {
        if (!reservedParams.includes(key) && value !== undefined && value !== null && value !== "") {
          let parsed: any = value
          if (value === "true") parsed = true
          else if (value === "false") parsed = false
          else if (!isNaN(Number(value)) && value !== "") parsed = Number(value)
          where[key] = parsed
        }
      }
      let include: any = undefined
      if (hooks.beforeList) {
        const result = await hooks.beforeList(ctx)
        if (result && !result.ok) return c.json({ error: result.error }, 400)
        if (result?.data) { where = result.data.where || where; include = result.data.include || include }
      }
      const items = await p.approvalRequest.findMany({
        where, include,
        take: query.limit ? parseInt(query.limit) : undefined,
        skip: query.offset ? parseInt(query.offset) : undefined,
      })
      return c.json({ ok: true, items })
    } catch (error: any) {
      console.error("[ApprovalRequest] List error:", error)
      return c.json({ error: { code: "list_failed", message: error.message } }, 500)
    }
  })

  router.get("/:id", async (c) => {
    try {
      const id = c.req.param("id")
      const ctx = buildContext(c)
      const p = getPrisma()
      if (hooks.beforeGet) {
        const result = await hooks.beforeGet(id, ctx)
        if (result && !result.ok) return c.json({ error: result.error }, result.error?.code === "not_found" ? 404 : 400)
      }
      const item = await p.approvalRequest.findUnique({ where: { id } })
      if (!item) return c.json({ error: { code: "not_found", message: "ApprovalRequest not found" } }, 404)
      return c.json({ ok: true, data: item })
    } catch (error: any) {
      console.error("[ApprovalRequest] Get error:", error)
      return c.json({ error: { code: "get_failed", message: error.message } }, 500)
    }
  })

  router.post("/", async (c) => {
    try {
      let body = await c.req.json()
      const ctx = buildContext(c, body)
      const p = getPrisma()
      if (hooks.beforeCreate) {
        const result = await hooks.beforeCreate(body, ctx)
        if (result && !result.ok) return c.json({ error: result.error }, 400)
        if (result?.data) body = result.data
      }
      const item = await p.approvalRequest.create({ data: body })
      if (hooks.afterCreate) await hooks.afterCreate(item, ctx)
      return c.json({ ok: true, data: item }, 201)
    } catch (error: any) {
      console.error("[ApprovalRequest] Create error:", error)
      return c.json({ error: { code: "create_failed", message: error.message } }, 500)
    }
  })

  router.patch("/:id", async (c) => {
    try {
      const id = c.req.param("id")
      let body = await c.req.json()
      const ctx = buildContext(c, body)
      const p = getPrisma()
      if (hooks.beforeUpdate) {
        const result = await hooks.beforeUpdate(id, body, ctx)
        if (result && !result.ok) return c.json({ error: result.error }, 400)
        if (result?.data) body = result.data
      }
      const item = await p.approvalRequest.update({ where: { id }, data: body })
      if (hooks.afterUpdate) await hooks.afterUpdate(item, ctx)
      return c.json({ ok: true, data: item })
    } catch (error: any) {
      console.error("[ApprovalRequest] Update error:", error)
      return c.json({ error: { code: "update_failed", message: error.message } }, 500)
    }
  })

  router.delete("/:id", async (c) => {
    try {
      const id = c.req.param("id")
      const ctx = buildContext(c)
      const p = getPrisma()
      if (hooks.beforeDelete) {
        const result = await hooks.beforeDelete(id, ctx)
        if (result && !result.ok) return c.json({ error: result.error }, 400)
      }
      await p.approvalRequest.delete({ where: { id } })
      if (hooks.afterDelete) await hooks.afterDelete(id, ctx)
      return c.json({ ok: true })
    } catch (error: any) {
      console.error("[ApprovalRequest] Delete error:", error)
      return c.json({ error: { code: "delete_failed", message: error.message } }, 500)
    }
  })

  return router
}
