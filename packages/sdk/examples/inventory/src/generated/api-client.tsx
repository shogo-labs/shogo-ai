/**
 * Auto-generated API Client
 *
 * Typed HTTP client for CRUD operations.
 * Generated by `shogo generate` - DO NOT EDIT
 */

import type {
  UserType,
  UserCreateInput,
  UserUpdateInput,
  CategoryType,
  CategoryCreateInput,
  CategoryUpdateInput,
  SupplierType,
  SupplierCreateInput,
  SupplierUpdateInput,
  ProductType,
  ProductCreateInput,
  ProductUpdateInput,
  StockMovementType,
  StockMovementCreateInput,
  StockMovementUpdateInput,
} from './types'

// ============================================================================
// Response Types
// ============================================================================

export interface ApiResponse<T> {
  ok: boolean
  data?: T
  items?: T[]
  error?: { code: string; message: string }
}

// ============================================================================
// Client Configuration
// ============================================================================

export interface ApiClientConfig {
  /** Base URL for API requests (e.g., "http://localhost:3000/api") */
  baseUrl: string
  /** Optional auth token to include in requests */
  token?: string
  /** Optional user ID to include in requests */
  userId?: string
}

let config: ApiClientConfig = { baseUrl: '/api' }

/**
 * Configure the API client
 */
export function configureApiClient(newConfig: Partial<ApiClientConfig>) {
  config = { ...config, ...newConfig }
}

/**
 * Get current API client configuration
 */
export function getApiConfig(): ApiClientConfig {
  return { ...config }
}

// ============================================================================
// HTTP Helpers
// ============================================================================

async function request<T>(
  method: string,
  path: string,
  body?: unknown
): Promise<ApiResponse<T>> {
  const headers: Record<string, string> = {
    'Content-Type': 'application/json',
  }

  if (config.token) {
    headers['Authorization'] = `Bearer ${config.token}`
  }

  const url = `${config.baseUrl}${path}`

  try {
    const response = await fetch(url, {
      method,
      headers,
      body: body ? JSON.stringify(body) : undefined,
    })

    const data = await response.json()

    if (!response.ok) {
      return {
        ok: false,
        error: data.error || { code: 'request_failed', message: `HTTP ${response.status}` },
      }
    }

    return data
  } catch (error) {
    return {
      ok: false,
      error: {
        code: 'network_error',
        message: error instanceof Error ? error.message : 'Network error',
      },
    }
  }
}

// ============================================================================
// User API Client
// ============================================================================

export const userApi = {
  /**
   * List all User records
   * @param options - Query options including where filters, pagination, and custom query params
   * @param options.where - Filter conditions (passed as query params to the API)
   * @param options.limit - Maximum number of records to return
   * @param options.offset - Number of records to skip
   * @param options.params - Additional query parameters to append to the request
   */
  async list(options?: { where?: Record<string, unknown>; limit?: number; offset?: number; params?: Record<string, string | number | boolean> }): Promise<ApiResponse<UserType[]>> {
    const params = new URLSearchParams()
    
    // Add where filters as query params
    if (options?.where) {
      for (const [key, value] of Object.entries(options.where)) {
        if (value !== undefined && value !== null) {
          params.set(key, String(value))
        }
      }
    }
    
    // Add additional params
    if (options?.params) {
      for (const [key, value] of Object.entries(options.params)) {
        if (value !== undefined && value !== null) {
          params.set(key, String(value))
        }
      }
    }
    
    // Add pagination
    if (options?.limit) params.set('limit', String(options.limit))
    if (options?.offset) params.set('offset', String(options.offset))
    if (config.userId) params.set('userId', config.userId)
    
    const query = params.toString() ? `?${params.toString()}` : ''
    const result = await request<UserType>('GET', `/users${query}`)
    return { ok: result.ok, items: result.items, error: result.error }
  },

  /**
   * Get a single User by ID
   */
  async get(id: string): Promise<ApiResponse<UserType>> {
    return request<UserType>('GET', `/users/${id}`)
  },

  /**
   * Create a new User
   */
  async create(input: UserCreateInput): Promise<ApiResponse<UserType>> {
    const body = config.userId ? { ...input, userId: config.userId } : input
    return request<UserType>('POST', `/users`, body)
  },

  /**
   * Update an existing User
   */
  async update(id: string, input: UserUpdateInput): Promise<ApiResponse<UserType>> {
    return request<UserType>('PATCH', `/users/${id}`, input)
  },

  /**
   * Delete a User
   */
  async delete(id: string): Promise<ApiResponse<void>> {
    return request<void>('DELETE', `/users/${id}`)
  },
}

// ============================================================================
// Category API Client
// ============================================================================

export const categoryApi = {
  /**
   * List all Category records
   * @param options - Query options including where filters, pagination, and custom query params
   * @param options.where - Filter conditions (passed as query params to the API)
   * @param options.limit - Maximum number of records to return
   * @param options.offset - Number of records to skip
   * @param options.params - Additional query parameters to append to the request
   */
  async list(options?: { where?: Record<string, unknown>; limit?: number; offset?: number; params?: Record<string, string | number | boolean> }): Promise<ApiResponse<CategoryType[]>> {
    const params = new URLSearchParams()
    
    // Add where filters as query params
    if (options?.where) {
      for (const [key, value] of Object.entries(options.where)) {
        if (value !== undefined && value !== null) {
          params.set(key, String(value))
        }
      }
    }
    
    // Add additional params
    if (options?.params) {
      for (const [key, value] of Object.entries(options.params)) {
        if (value !== undefined && value !== null) {
          params.set(key, String(value))
        }
      }
    }
    
    // Add pagination
    if (options?.limit) params.set('limit', String(options.limit))
    if (options?.offset) params.set('offset', String(options.offset))
    if (config.userId) params.set('userId', config.userId)
    
    const query = params.toString() ? `?${params.toString()}` : ''
    const result = await request<CategoryType>('GET', `/categories${query}`)
    return { ok: result.ok, items: result.items, error: result.error }
  },

  /**
   * Get a single Category by ID
   */
  async get(id: string): Promise<ApiResponse<CategoryType>> {
    return request<CategoryType>('GET', `/categories/${id}`)
  },

  /**
   * Create a new Category
   */
  async create(input: CategoryCreateInput): Promise<ApiResponse<CategoryType>> {
    const body = config.userId ? { ...input, userId: config.userId } : input
    return request<CategoryType>('POST', `/categories`, body)
  },

  /**
   * Update an existing Category
   */
  async update(id: string, input: CategoryUpdateInput): Promise<ApiResponse<CategoryType>> {
    return request<CategoryType>('PATCH', `/categories/${id}`, input)
  },

  /**
   * Delete a Category
   */
  async delete(id: string): Promise<ApiResponse<void>> {
    return request<void>('DELETE', `/categories/${id}`)
  },
}

// ============================================================================
// Supplier API Client
// ============================================================================

export const supplierApi = {
  /**
   * List all Supplier records
   * @param options - Query options including where filters, pagination, and custom query params
   * @param options.where - Filter conditions (passed as query params to the API)
   * @param options.limit - Maximum number of records to return
   * @param options.offset - Number of records to skip
   * @param options.params - Additional query parameters to append to the request
   */
  async list(options?: { where?: Record<string, unknown>; limit?: number; offset?: number; params?: Record<string, string | number | boolean> }): Promise<ApiResponse<SupplierType[]>> {
    const params = new URLSearchParams()
    
    // Add where filters as query params
    if (options?.where) {
      for (const [key, value] of Object.entries(options.where)) {
        if (value !== undefined && value !== null) {
          params.set(key, String(value))
        }
      }
    }
    
    // Add additional params
    if (options?.params) {
      for (const [key, value] of Object.entries(options.params)) {
        if (value !== undefined && value !== null) {
          params.set(key, String(value))
        }
      }
    }
    
    // Add pagination
    if (options?.limit) params.set('limit', String(options.limit))
    if (options?.offset) params.set('offset', String(options.offset))
    if (config.userId) params.set('userId', config.userId)
    
    const query = params.toString() ? `?${params.toString()}` : ''
    const result = await request<SupplierType>('GET', `/suppliers${query}`)
    return { ok: result.ok, items: result.items, error: result.error }
  },

  /**
   * Get a single Supplier by ID
   */
  async get(id: string): Promise<ApiResponse<SupplierType>> {
    return request<SupplierType>('GET', `/suppliers/${id}`)
  },

  /**
   * Create a new Supplier
   */
  async create(input: SupplierCreateInput): Promise<ApiResponse<SupplierType>> {
    const body = config.userId ? { ...input, userId: config.userId } : input
    return request<SupplierType>('POST', `/suppliers`, body)
  },

  /**
   * Update an existing Supplier
   */
  async update(id: string, input: SupplierUpdateInput): Promise<ApiResponse<SupplierType>> {
    return request<SupplierType>('PATCH', `/suppliers/${id}`, input)
  },

  /**
   * Delete a Supplier
   */
  async delete(id: string): Promise<ApiResponse<void>> {
    return request<void>('DELETE', `/suppliers/${id}`)
  },
}

// ============================================================================
// Product API Client
// ============================================================================

export const productApi = {
  /**
   * List all Product records
   * @param options - Query options including where filters, pagination, and custom query params
   * @param options.where - Filter conditions (passed as query params to the API)
   * @param options.limit - Maximum number of records to return
   * @param options.offset - Number of records to skip
   * @param options.params - Additional query parameters to append to the request
   */
  async list(options?: { where?: Record<string, unknown>; limit?: number; offset?: number; params?: Record<string, string | number | boolean> }): Promise<ApiResponse<ProductType[]>> {
    const params = new URLSearchParams()
    
    // Add where filters as query params
    if (options?.where) {
      for (const [key, value] of Object.entries(options.where)) {
        if (value !== undefined && value !== null) {
          params.set(key, String(value))
        }
      }
    }
    
    // Add additional params
    if (options?.params) {
      for (const [key, value] of Object.entries(options.params)) {
        if (value !== undefined && value !== null) {
          params.set(key, String(value))
        }
      }
    }
    
    // Add pagination
    if (options?.limit) params.set('limit', String(options.limit))
    if (options?.offset) params.set('offset', String(options.offset))
    if (config.userId) params.set('userId', config.userId)
    
    const query = params.toString() ? `?${params.toString()}` : ''
    const result = await request<ProductType>('GET', `/products${query}`)
    return { ok: result.ok, items: result.items, error: result.error }
  },

  /**
   * Get a single Product by ID
   */
  async get(id: string): Promise<ApiResponse<ProductType>> {
    return request<ProductType>('GET', `/products/${id}`)
  },

  /**
   * Create a new Product
   */
  async create(input: ProductCreateInput): Promise<ApiResponse<ProductType>> {
    const body = config.userId ? { ...input, userId: config.userId } : input
    return request<ProductType>('POST', `/products`, body)
  },

  /**
   * Update an existing Product
   */
  async update(id: string, input: ProductUpdateInput): Promise<ApiResponse<ProductType>> {
    return request<ProductType>('PATCH', `/products/${id}`, input)
  },

  /**
   * Delete a Product
   */
  async delete(id: string): Promise<ApiResponse<void>> {
    return request<void>('DELETE', `/products/${id}`)
  },
}

// ============================================================================
// StockMovement API Client
// ============================================================================

export const stockMovementApi = {
  /**
   * List all StockMovement records
   * @param options - Query options including where filters, pagination, and custom query params
   * @param options.where - Filter conditions (passed as query params to the API)
   * @param options.limit - Maximum number of records to return
   * @param options.offset - Number of records to skip
   * @param options.params - Additional query parameters to append to the request
   */
  async list(options?: { where?: Record<string, unknown>; limit?: number; offset?: number; params?: Record<string, string | number | boolean> }): Promise<ApiResponse<StockMovementType[]>> {
    const params = new URLSearchParams()
    
    // Add where filters as query params
    if (options?.where) {
      for (const [key, value] of Object.entries(options.where)) {
        if (value !== undefined && value !== null) {
          params.set(key, String(value))
        }
      }
    }
    
    // Add additional params
    if (options?.params) {
      for (const [key, value] of Object.entries(options.params)) {
        if (value !== undefined && value !== null) {
          params.set(key, String(value))
        }
      }
    }
    
    // Add pagination
    if (options?.limit) params.set('limit', String(options.limit))
    if (options?.offset) params.set('offset', String(options.offset))
    if (config.userId) params.set('userId', config.userId)
    
    const query = params.toString() ? `?${params.toString()}` : ''
    const result = await request<StockMovementType>('GET', `/stock-movements${query}`)
    return { ok: result.ok, items: result.items, error: result.error }
  },

  /**
   * Get a single StockMovement by ID
   */
  async get(id: string): Promise<ApiResponse<StockMovementType>> {
    return request<StockMovementType>('GET', `/stock-movements/${id}`)
  },

  /**
   * Create a new StockMovement
   */
  async create(input: StockMovementCreateInput): Promise<ApiResponse<StockMovementType>> {
    const body = config.userId ? { ...input, userId: config.userId } : input
    return request<StockMovementType>('POST', `/stock-movements`, body)
  },

  /**
   * Update an existing StockMovement
   */
  async update(id: string, input: StockMovementUpdateInput): Promise<ApiResponse<StockMovementType>> {
    return request<StockMovementType>('PATCH', `/stock-movements/${id}`, input)
  },

  /**
   * Delete a StockMovement
   */
  async delete(id: string): Promise<ApiResponse<void>> {
    return request<void>('DELETE', `/stock-movements/${id}`)
  },
}

// ============================================================================
// Combined API Client
// ============================================================================

/**
 * Combined API client with all model endpoints
 */
export const api = {
  user: userApi,
  category: categoryApi,
  supplier: supplierApi,
  product: productApi,
  stockMovement: stockMovementApi,
}

export default api
