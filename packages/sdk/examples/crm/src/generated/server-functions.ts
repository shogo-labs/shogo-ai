/**
 * Auto-generated Server Functions
 *
 * CRUD operations for TanStack Start.
 * Generated by `shogo generate` - DO NOT EDIT
 */

import { createServerFn } from '@tanstack/react-start'
import { shogo } from '../lib/shogo'
import { hooks } from './hooks'
import type {
  UserType,
  UserCreateInput,
  UserUpdateInput,
  ContactType,
  ContactCreateInput,
  ContactUpdateInput,
  CompanyType,
  CompanyCreateInput,
  CompanyUpdateInput,
  TagType,
  TagCreateInput,
  TagUpdateInput,
  ContactTagType,
  ContactTagCreateInput,
  ContactTagUpdateInput,
  NoteType,
  NoteCreateInput,
  NoteUpdateInput,
  DealType,
  DealCreateInput,
  DealUpdateInput,
} from './types'

// ============================================================================
// User Server Functions
// ============================================================================

/**
 * List all User records
 */
export const getUserList = createServerFn({ method: 'POST' })
  .inputValidator((data: { userId?: string; where?: Record<string, unknown> }) => data)
  .handler(async ({ data }) => {
    let where: Record<string, unknown> = data.where || {}

    // Apply beforeList hook if defined
    const hook = hooks.User?.beforeList
    if (hook) {
      const hookResult = await hook({ userId: data.userId })
      if (hookResult?.where) {
        where = { ...where, ...hookResult.where }
      }
    }

    const items = await shogo.db.user.findMany({
      where,
      orderBy: { createdAt: 'desc' },
    })

    return items as UserType[]
  })

/**
 * Get a single User by ID
 */
export const getUserById = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeGet hook if defined
    const hook = hooks.User?.beforeGet
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    const item = await shogo.db.user.findUnique({
      where: { id: data.id },
    })

    if (!item) {
      throw new Error('User not found')
    }

    return item as UserType
  })

/**
 * Create a new User
 */
export const createUser = createServerFn({ method: 'POST' })
  .inputValidator((data: { input: UserCreateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeCreate hook if defined
    const hook = hooks.User?.beforeCreate
    if (hook) {
      const hookResult = await hook(input, { userId: data.userId })
      if (hookResult) {
        input = hookResult
      }
    }

    const item = await shogo.db.user.create({
      data: input,
    })

    // Apply afterCreate hook if defined
    const afterHook = hooks.User?.afterCreate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as UserType
  })

/**
 * Update an existing User
 */
export const updateUser = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; input: UserUpdateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeUpdate hook if defined
    const hook = hooks.User?.beforeUpdate
    if (hook) {
      const hookResult = await hook(data.id, input, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
      if (hookResult?.input) {
        input = hookResult.input
      }
    }

    const item = await shogo.db.user.update({
      where: { id: data.id },
      data: input,
    })

    // Apply afterUpdate hook if defined
    const afterHook = hooks.User?.afterUpdate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as UserType
  })

/**
 * Delete a User
 */
export const deleteUser = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeDelete hook if defined
    const hook = hooks.User?.beforeDelete
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    await shogo.db.user.delete({
      where: { id: data.id },
    })

    // Apply afterDelete hook if defined
    const afterHook = hooks.User?.afterDelete
    if (afterHook) {
      await afterHook(data.id, { userId: data.userId })
    }

    return { success: true }
  })

// ============================================================================
// Contact Server Functions
// ============================================================================

/**
 * List all Contact records
 */
export const getContactList = createServerFn({ method: 'POST' })
  .inputValidator((data: { userId?: string; where?: Record<string, unknown> }) => data)
  .handler(async ({ data }) => {
    let where: Record<string, unknown> = data.where || {}

    // Apply beforeList hook if defined
    const hook = hooks.Contact?.beforeList
    if (hook) {
      const hookResult = await hook({ userId: data.userId })
      if (hookResult?.where) {
        where = { ...where, ...hookResult.where }
      }
    }

    const items = await shogo.db.contact.findMany({
      where,
      orderBy: { createdAt: 'desc' },
    })

    return items as ContactType[]
  })

/**
 * Get a single Contact by ID
 */
export const getContactById = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeGet hook if defined
    const hook = hooks.Contact?.beforeGet
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    const item = await shogo.db.contact.findUnique({
      where: { id: data.id },
    })

    if (!item) {
      throw new Error('Contact not found')
    }

    return item as ContactType
  })

/**
 * Create a new Contact
 */
export const createContact = createServerFn({ method: 'POST' })
  .inputValidator((data: { input: ContactCreateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeCreate hook if defined
    const hook = hooks.Contact?.beforeCreate
    if (hook) {
      const hookResult = await hook(input, { userId: data.userId })
      if (hookResult) {
        input = hookResult
      }
    }

    const item = await shogo.db.contact.create({
      data: input,
    })

    // Apply afterCreate hook if defined
    const afterHook = hooks.Contact?.afterCreate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as ContactType
  })

/**
 * Update an existing Contact
 */
export const updateContact = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; input: ContactUpdateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeUpdate hook if defined
    const hook = hooks.Contact?.beforeUpdate
    if (hook) {
      const hookResult = await hook(data.id, input, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
      if (hookResult?.input) {
        input = hookResult.input
      }
    }

    const item = await shogo.db.contact.update({
      where: { id: data.id },
      data: input,
    })

    // Apply afterUpdate hook if defined
    const afterHook = hooks.Contact?.afterUpdate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as ContactType
  })

/**
 * Delete a Contact
 */
export const deleteContact = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeDelete hook if defined
    const hook = hooks.Contact?.beforeDelete
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    await shogo.db.contact.delete({
      where: { id: data.id },
    })

    // Apply afterDelete hook if defined
    const afterHook = hooks.Contact?.afterDelete
    if (afterHook) {
      await afterHook(data.id, { userId: data.userId })
    }

    return { success: true }
  })

// ============================================================================
// Company Server Functions
// ============================================================================

/**
 * List all Company records
 */
export const getCompanyList = createServerFn({ method: 'POST' })
  .inputValidator((data: { userId?: string; where?: Record<string, unknown> }) => data)
  .handler(async ({ data }) => {
    let where: Record<string, unknown> = data.where || {}

    // Apply beforeList hook if defined
    const hook = hooks.Company?.beforeList
    if (hook) {
      const hookResult = await hook({ userId: data.userId })
      if (hookResult?.where) {
        where = { ...where, ...hookResult.where }
      }
    }

    const items = await shogo.db.company.findMany({
      where,
      orderBy: { createdAt: 'desc' },
    })

    return items as CompanyType[]
  })

/**
 * Get a single Company by ID
 */
export const getCompanyById = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeGet hook if defined
    const hook = hooks.Company?.beforeGet
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    const item = await shogo.db.company.findUnique({
      where: { id: data.id },
    })

    if (!item) {
      throw new Error('Company not found')
    }

    return item as CompanyType
  })

/**
 * Create a new Company
 */
export const createCompany = createServerFn({ method: 'POST' })
  .inputValidator((data: { input: CompanyCreateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeCreate hook if defined
    const hook = hooks.Company?.beforeCreate
    if (hook) {
      const hookResult = await hook(input, { userId: data.userId })
      if (hookResult) {
        input = hookResult
      }
    }

    const item = await shogo.db.company.create({
      data: input,
    })

    // Apply afterCreate hook if defined
    const afterHook = hooks.Company?.afterCreate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as CompanyType
  })

/**
 * Update an existing Company
 */
export const updateCompany = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; input: CompanyUpdateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeUpdate hook if defined
    const hook = hooks.Company?.beforeUpdate
    if (hook) {
      const hookResult = await hook(data.id, input, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
      if (hookResult?.input) {
        input = hookResult.input
      }
    }

    const item = await shogo.db.company.update({
      where: { id: data.id },
      data: input,
    })

    // Apply afterUpdate hook if defined
    const afterHook = hooks.Company?.afterUpdate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as CompanyType
  })

/**
 * Delete a Company
 */
export const deleteCompany = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeDelete hook if defined
    const hook = hooks.Company?.beforeDelete
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    await shogo.db.company.delete({
      where: { id: data.id },
    })

    // Apply afterDelete hook if defined
    const afterHook = hooks.Company?.afterDelete
    if (afterHook) {
      await afterHook(data.id, { userId: data.userId })
    }

    return { success: true }
  })

// ============================================================================
// Tag Server Functions
// ============================================================================

/**
 * List all Tag records
 */
export const getTagList = createServerFn({ method: 'POST' })
  .inputValidator((data: { userId?: string; where?: Record<string, unknown> }) => data)
  .handler(async ({ data }) => {
    let where: Record<string, unknown> = data.where || {}

    // Apply beforeList hook if defined
    const hook = hooks.Tag?.beforeList
    if (hook) {
      const hookResult = await hook({ userId: data.userId })
      if (hookResult?.where) {
        where = { ...where, ...hookResult.where }
      }
    }

    const items = await shogo.db.tag.findMany({
      where,
      orderBy: { createdAt: 'desc' },
    })

    return items as TagType[]
  })

/**
 * Get a single Tag by ID
 */
export const getTagById = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeGet hook if defined
    const hook = hooks.Tag?.beforeGet
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    const item = await shogo.db.tag.findUnique({
      where: { id: data.id },
    })

    if (!item) {
      throw new Error('Tag not found')
    }

    return item as TagType
  })

/**
 * Create a new Tag
 */
export const createTag = createServerFn({ method: 'POST' })
  .inputValidator((data: { input: TagCreateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeCreate hook if defined
    const hook = hooks.Tag?.beforeCreate
    if (hook) {
      const hookResult = await hook(input, { userId: data.userId })
      if (hookResult) {
        input = hookResult
      }
    }

    const item = await shogo.db.tag.create({
      data: input,
    })

    // Apply afterCreate hook if defined
    const afterHook = hooks.Tag?.afterCreate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as TagType
  })

/**
 * Update an existing Tag
 */
export const updateTag = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; input: TagUpdateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeUpdate hook if defined
    const hook = hooks.Tag?.beforeUpdate
    if (hook) {
      const hookResult = await hook(data.id, input, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
      if (hookResult?.input) {
        input = hookResult.input
      }
    }

    const item = await shogo.db.tag.update({
      where: { id: data.id },
      data: input,
    })

    // Apply afterUpdate hook if defined
    const afterHook = hooks.Tag?.afterUpdate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as TagType
  })

/**
 * Delete a Tag
 */
export const deleteTag = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeDelete hook if defined
    const hook = hooks.Tag?.beforeDelete
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    await shogo.db.tag.delete({
      where: { id: data.id },
    })

    // Apply afterDelete hook if defined
    const afterHook = hooks.Tag?.afterDelete
    if (afterHook) {
      await afterHook(data.id, { userId: data.userId })
    }

    return { success: true }
  })

// Skipped ContactTag - no @id field found

// ============================================================================
// Note Server Functions
// ============================================================================

/**
 * List all Note records
 */
export const getNoteList = createServerFn({ method: 'POST' })
  .inputValidator((data: { userId?: string; where?: Record<string, unknown> }) => data)
  .handler(async ({ data }) => {
    let where: Record<string, unknown> = data.where || {}

    // Apply beforeList hook if defined
    const hook = hooks.Note?.beforeList
    if (hook) {
      const hookResult = await hook({ userId: data.userId })
      if (hookResult?.where) {
        where = { ...where, ...hookResult.where }
      }
    }

    const items = await shogo.db.note.findMany({
      where,
      orderBy: { createdAt: 'desc' },
    })

    return items as NoteType[]
  })

/**
 * Get a single Note by ID
 */
export const getNoteById = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeGet hook if defined
    const hook = hooks.Note?.beforeGet
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    const item = await shogo.db.note.findUnique({
      where: { id: data.id },
    })

    if (!item) {
      throw new Error('Note not found')
    }

    return item as NoteType
  })

/**
 * Create a new Note
 */
export const createNote = createServerFn({ method: 'POST' })
  .inputValidator((data: { input: NoteCreateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeCreate hook if defined
    const hook = hooks.Note?.beforeCreate
    if (hook) {
      const hookResult = await hook(input, { userId: data.userId })
      if (hookResult) {
        input = hookResult
      }
    }

    const item = await shogo.db.note.create({
      data: input,
    })

    // Apply afterCreate hook if defined
    const afterHook = hooks.Note?.afterCreate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as NoteType
  })

/**
 * Update an existing Note
 */
export const updateNote = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; input: NoteUpdateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeUpdate hook if defined
    const hook = hooks.Note?.beforeUpdate
    if (hook) {
      const hookResult = await hook(data.id, input, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
      if (hookResult?.input) {
        input = hookResult.input
      }
    }

    const item = await shogo.db.note.update({
      where: { id: data.id },
      data: input,
    })

    // Apply afterUpdate hook if defined
    const afterHook = hooks.Note?.afterUpdate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as NoteType
  })

/**
 * Delete a Note
 */
export const deleteNote = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeDelete hook if defined
    const hook = hooks.Note?.beforeDelete
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    await shogo.db.note.delete({
      where: { id: data.id },
    })

    // Apply afterDelete hook if defined
    const afterHook = hooks.Note?.afterDelete
    if (afterHook) {
      await afterHook(data.id, { userId: data.userId })
    }

    return { success: true }
  })

// ============================================================================
// Deal Server Functions
// ============================================================================

/**
 * List all Deal records
 */
export const getDealList = createServerFn({ method: 'POST' })
  .inputValidator((data: { userId?: string; where?: Record<string, unknown> }) => data)
  .handler(async ({ data }) => {
    let where: Record<string, unknown> = data.where || {}

    // Apply beforeList hook if defined
    const hook = hooks.Deal?.beforeList
    if (hook) {
      const hookResult = await hook({ userId: data.userId })
      if (hookResult?.where) {
        where = { ...where, ...hookResult.where }
      }
    }

    const items = await shogo.db.deal.findMany({
      where,
      orderBy: { createdAt: 'desc' },
    })

    return items as DealType[]
  })

/**
 * Get a single Deal by ID
 */
export const getDealById = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeGet hook if defined
    const hook = hooks.Deal?.beforeGet
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    const item = await shogo.db.deal.findUnique({
      where: { id: data.id },
    })

    if (!item) {
      throw new Error('Deal not found')
    }

    return item as DealType
  })

/**
 * Create a new Deal
 */
export const createDeal = createServerFn({ method: 'POST' })
  .inputValidator((data: { input: DealCreateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeCreate hook if defined
    const hook = hooks.Deal?.beforeCreate
    if (hook) {
      const hookResult = await hook(input, { userId: data.userId })
      if (hookResult) {
        input = hookResult
      }
    }

    const item = await shogo.db.deal.create({
      data: input,
    })

    // Apply afterCreate hook if defined
    const afterHook = hooks.Deal?.afterCreate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as DealType
  })

/**
 * Update an existing Deal
 */
export const updateDeal = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; input: DealUpdateInput; userId?: string }) => data)
  .handler(async ({ data }) => {
    let input = data.input

    // Apply beforeUpdate hook if defined
    const hook = hooks.Deal?.beforeUpdate
    if (hook) {
      const hookResult = await hook(data.id, input, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
      if (hookResult?.input) {
        input = hookResult.input
      }
    }

    const item = await shogo.db.deal.update({
      where: { id: data.id },
      data: input,
    })

    // Apply afterUpdate hook if defined
    const afterHook = hooks.Deal?.afterUpdate
    if (afterHook) {
      await afterHook(item, { userId: data.userId })
    }

    return item as DealType
  })

/**
 * Delete a Deal
 */
export const deleteDeal = createServerFn({ method: 'POST' })
  .inputValidator((data: { id: string; userId?: string }) => data)
  .handler(async ({ data }) => {
    // Apply beforeDelete hook if defined
    const hook = hooks.Deal?.beforeDelete
    if (hook) {
      const hookResult = await hook(data.id, { userId: data.userId })
      if (hookResult?.deny) {
        throw new Error('Access denied')
      }
    }

    await shogo.db.deal.delete({
      where: { id: data.id },
    })

    // Apply afterDelete hook if defined
    const afterHook = hooks.Deal?.afterDelete
    if (afterHook) {
      await afterHook(data.id, { userId: data.userId })
    }

    return { success: true }
  })
