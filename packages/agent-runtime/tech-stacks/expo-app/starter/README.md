# Expo (React Native) Starter

Cross-platform mobile app scaffold powered by Expo Router.

```
app/
  _layout.tsx   # Root stack
  index.tsx     # Home screen
```

The Metro bundler is started by the runtime; do not run `expo start` from
the agent's `exec` tool. Real-device preview is published via a tunneled
URL surfaced in the Studio UI.
