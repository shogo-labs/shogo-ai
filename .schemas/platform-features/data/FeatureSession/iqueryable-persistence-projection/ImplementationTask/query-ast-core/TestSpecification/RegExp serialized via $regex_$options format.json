{
  "id": "test-serialize-regex",
  "scenario": "RegExp serialized via $regex/$options format",
  "given": [
    "Condition with RegExp value: { name: { $regex: /test/i } }"
  ],
  "when": "Serializing and deserializing the condition",
  "then": [
    "Serialized form uses { $regex: 'test', $options: 'i' } format",
    "No native RegExp in serialized output",
    "Deserialized condition reconstructs equivalent RegExp"
  ],
  "testType": "unit",
  "targetFile": "packages/state-api/src/query/ast/__tests__/serialization.test.ts",
  "createdAt": 1734106500018,
  "task": "task-query-ast-core",
  "requirement": "req-02-mongodb-operators"
}