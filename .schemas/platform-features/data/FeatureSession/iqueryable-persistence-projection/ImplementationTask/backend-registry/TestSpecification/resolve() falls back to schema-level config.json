{
  "id": "test-registry-resolve-schema",
  "scenario": "resolve() falls back to schema-level config",
  "given": [
    "BackendRegistry with meta-store",
    "Model has no x-persistence.backend",
    "Schema has x-persistence.backend: 'memory'"
  ],
  "when": "Calling resolve(schemaName, modelName)",
  "then": [
    "Returns 'memory' backend",
    "Falls back to schema config",
    "Default not used"
  ],
  "testType": "unit",
  "targetFile": "packages/state-api/src/query/__tests__/registry.test.ts",
  "createdAt": 1734106500116,
  "task": "task-backend-registry",
  "requirement": "req-04-schema-driven-binding"
}