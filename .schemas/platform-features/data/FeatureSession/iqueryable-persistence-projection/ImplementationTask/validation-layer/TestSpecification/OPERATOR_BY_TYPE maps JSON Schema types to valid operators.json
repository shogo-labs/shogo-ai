{
  "id": "test-validation-operator-map",
  "scenario": "OPERATOR_BY_TYPE maps JSON Schema types to valid operators",
  "given": [
    "OPERATOR_BY_TYPE constant is imported"
  ],
  "when": "Inspecting operator mappings",
  "then": [
    "string type includes $eq, $ne, $in, $regex, $contains",
    "number type includes $eq, $ne, $gt, $gte, $lt, $lte, $in",
    "boolean type includes only $eq, $ne",
    "array type includes $in, $nin, $contains"
  ],
  "testType": "unit",
  "targetFile": "packages/state-api/src/query/validation/__tests__/types.test.ts",
  "createdAt": 1734106500043,
  "task": "task-validation-layer",
  "requirement": "req-05-metastore-validation"
}