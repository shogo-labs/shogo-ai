{
  "id": "test-p2-bun-executor-02",
  "scenario": "execute converts parameterized SQL to tagged template invocation",
  "given": [
    "BunSqlExecutor instance with test database connection",
    "SQL string: 'SELECT * FROM users WHERE id = $1'",
    "Params array: [123]"
  ],
  "when": "execute(['SELECT * FROM users WHERE id = $1', [123]]) is called",
  "then": [
    "SQL is split on $N placeholders via sql.split(/\\$\\d+/)",
    "Tagged template sql(parts, ...params) is invoked",
    "Query executes against database",
    "Result rows are returned"
  ],
  "testType": "integration",
  "targetFile": "packages/state-api/src/query/execution/__tests__/bun-sql-executor.test.ts",
  "createdAt": 1734278600021,
  "task": "task-p2-bun-sql-executor",
  "requirement": "req-08-database-executor"
}